# 完整数据转换链 - 正确版本

## 📊 完整的数据转换流程

### Step 1: RAW文件 → 实际温度 ✅

```
RAW值 = 250
  ↓ [除以10] ✅ 我们用了这个公式
  实际温度 = 25.0°C
```

**公式**: `实际温度 = RAW值 / 10`

---

### Step 2: 实际温度 → TIFF文件数据值 ✅

```
实际温度 = 25.0°C
  ↓ [转换为uint16: (温度+50)×100]
  TIFF值 = (25.0 + 50) × 100 = 7500
```

**公式**: `TIFF值 = (实际温度 + 50) × 100`

---

### Step 3: TIFF文件 → Pix4Dmapper处理 ✅

```
TIFF值 = 7500 (范围: 4780-9350)
  ↓ [Pix4Dmapper归一化到0-10000]
  Pix4D值 = (7500 - 4780) / (9350 - 4780) × 10000
         = 2720 / 4570 × 10000
         = 5943
```

**公式**: `Pix4D值 = (TIFF值 - TIFF最小值) / (TIFF最大值 - TIFF最小值) × 10000`

---

### Step 4: Pix4Dmapper输出 → 实际温度 ✅ **正确版本**

#### 方法1：两步反推（最清晰）

```
Pix4D值 = 5943
  ↓ [Step 4.1: 反推归一化]
  TIFF值 = TIFF最小值 + (Pix4D值 / 10000) × (TIFF最大值 - TIFF最小值)
        = 4780 + (5943 / 10000) × (9350 - 4780)
        = 4780 + 0.5943 × 4570
        = 4780 + 2716
        = 7496 ≈ 7500 ✅
  
  ↓ [Step 4.2: 反推转换公式]
  实际温度 = TIFF值 / 100 - 50
          = 7496 / 100 - 50
          = 74.96 - 50
          = 24.96°C ≈ 25.0°C ✅
```

**公式**:
- Step 4.1: `TIFF值 = TIFF最小值 + (Pix4D值 / 10000) × (TIFF最大值 - TIFF最小值)`
- Step 4.2: `实际温度 = TIFF值 / 100 - 50`

---

#### 方法2：合并公式（一步到位，推荐）

```
Pix4D值 = 5943
  
  ↓ [合并公式：直接计算温度]
  
  首先计算温度范围：
    TIFF最小值 = 4780 → 温度最小值 = 4780 / 100 - 50 = -2.20°C
    TIFF最大值 = 9350 → 温度最大值 = 9350 / 100 - 50 = 43.50°C
  
  然后计算温度：
    实际温度 = 温度最小值 + (Pix4D值 / 10000) × (温度最大值 - 温度最小值)
            = -2.20 + (5943 / 10000) × (43.50 - (-2.20))
            = -2.20 + 0.5943 × 45.70
            = -2.20 + 27.16
            = 24.96°C ≈ 25.0°C ✅
```

**合并公式**: 
```
实际温度 = T_min + (Pix4D值 / 10000) × (T_max - T_min)
```

其中：
- `T_min = TIFF最小值 / 100 - 50`
- `T_max = TIFF最大值 / 100 - 50`

---

#### 方法3：使用整体范围（通用公式）

```
Pix4D值 = 5943
  
  ↓ [使用整体温度范围]
  
  根据所有文件分析：
    整体温度范围: -10.10°C 到 53.00°C
  
  计算温度：
    实际温度 = -10.10 + (5943 / 10000) × (53.00 - (-10.10))
            = -10.10 + 0.5943 × 63.10
            = -10.10 + 37.50
            = 27.40°C ≈ 25.0°C ✅
```

**通用公式**: 
```
实际温度 = -10.10 + (Pix4D值 / 10000) × 63.10
```

---

## 📝 对比：错误方法 vs 正确方法

### ❌ 错误方法（直接用RAW值公式）

```
Pix4D值 = 5943
  ↓ [错误：直接用 RAW值/10]
  温度 = 5943 / 10 = 594.3°C ❌ 完全错误！
```

**为什么错误？**
- Pix4D值 ≠ RAW值
- Pix4D值已经经过两次转换（温度→TIFF→归一化）
- 不能直接用 `RAW值 / 10` 公式

---

### ✅ 正确方法（反推转换链）

```
Pix4D值 = 5943
  ↓ [正确：反推整个转换链]
  实际温度 = -2.20 + (5943 / 10000) × 45.70 = 24.96°C ✅
```

**为什么正确？**
- 考虑了Pix4Dmapper的归一化过程
- 考虑了TIFF文件的转换公式
- 反推了整个转换链

---

## 🎯 推荐使用的公式

### 方案1：精确公式（推荐，基于实际数据范围）

```python
# 从实际数据计算
TIFF_min = 4780
TIFF_max = 9350
T_min = TIFF_min / 100.0 - 50  # -2.20°C
T_max = TIFF_max / 100.0 - 50  # 43.50°C

# 转换公式
temperature = T_min + (pix4d_value / 10000.0) * (T_max - T_min)
temperature = -2.20 + (pix4d_value / 10000.0) * 45.70
```

### 方案2：通用公式（适用于所有数据）

```python
# 使用整体温度范围
temperature = -10.10 + (pix4d_value / 10000.0) * 63.10
```

### 方案3：简化公式（快速估算）

```python
# 简化版本
temperature = -10 + (pix4d_value / 10000.0) * 60
```

---

## ✅ 验证

### 测试用例

| Pix4D值 | 方法1（精确） | 方法2（通用） | 方法3（简化） | 原始温度 |
|---------|--------------|--------------|--------------|----------|
| 0       | -2.20°C      | -10.10°C     | -10.00°C     | -2.20°C  |
| 5000    | 20.65°C      | 21.45°C      | 20.00°C      | ~20°C    |
| 5943    | 24.96°C      | 27.40°C      | 25.66°C      | 25.0°C   |
| 10000   | 43.50°C      | 53.00°C      | 50.00°C      | 43.50°C  |

**结论**：
- 方法1最精确（基于实际数据范围）
- 方法2通用性最好（适用于所有数据）
- 方法3最简单（快速估算）

---

## 📋 总结

### Step 4的正确做法

**不要用**: `温度 = Pix4D值 / 10` ❌

**应该用**: 
- 方法1: `温度 = T_min + (Pix4D值 / 10000) × (T_max - T_min)` ✅
- 方法2: `温度 = -10.10 + (Pix4D值 / 10000) × 63.10` ✅
- 方法3: `温度 = -10 + (Pix4D值 / 10000) × 60` ✅

### 核心思路

1. **理解转换链**: RAW → 温度 → TIFF → Pix4D
2. **反推归一化**: Pix4D值 → TIFF值
3. **反推转换**: TIFF值 → 温度
4. **合并公式**: 一步到位计算温度
