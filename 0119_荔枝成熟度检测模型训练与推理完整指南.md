# 荔枝成熟度检测模型训练与推理完整指南

> **适合初学者** - 从零开始学习深度学习目标检测的完整流程

## 📚 目录

1. [基础概念](#基础概念)
2. [整体工作流程](#整体工作流程)
3. [训练流程详解](#训练流程详解)
4. [推理流程详解](#推理流程详解)
5. [代码详解](#代码详解)
6. [常见问题](#常见问题)
7. [实战示例](#实战示例)

---

## 一、基础概念

### 1.1 什么是训练（Training）？

**训练**是指让机器学习模型从数据中学习规律的过程。

**简单类比**：
- 就像教小孩认水果：给他看很多苹果、香蕉的图片，告诉他"这是苹果"、"这是香蕉"
- 经过大量练习后，小孩就能自己识别新图片中的水果

**在荔枝检测中**：
- **输入**：荔枝图像 + 标注信息（图像中哪些区域是"未成熟"、"转色期"、"着色期"、"成熟"）
- **过程**：模型通过大量图像学习不同成熟度荔枝的特征（颜色、形状等）
- **输出**：一个训练好的模型文件（如 `best.pt` 或 `v5.onnx`），这个文件包含了模型学到的"知识"

**训练的目标**：让模型能够准确识别图像中荔枝的成熟度阶段

### 1.2 什么是推理（Inference）？

**推理**就是使用训练好的模型对新图像进行预测的过程。

**简单类比**：
- 就像考试：学生已经学完了知识（训练完成），现在给他一张新试卷（新图像），让他答题（预测结果）
- 学生根据学到的知识，识别出图像中的荔枝属于哪个成熟度阶段

**在荔枝检测中**：
- **输入**：一张新的荔枝图像（从未见过的图像）
- **过程**：训练好的模型分析图像，找出所有荔枝，并判断每个荔枝的成熟度
- **输出**：检测结果
  - 每个荔枝的位置（边界框坐标）
  - 每个荔枝的成熟度类别（Immature/Turing/Coloring/Ripe）
  - 每个检测的置信度（模型对自己预测的把握程度，0-1之间）

**推理的目标**：使用学到的"知识"解决实际问题

### 1.3 训练 vs 推理的区别

| 特征 | 训练（Training） | 推理（Inference） |
|------|-----------------|------------------|
| **目的** | 让模型学习知识 | 使用模型解决问题 |
| **输入** | 大量标注好的图像 | 新的、未标注的图像 |
| **时间** | 很长（几小时到几天） | 很短（毫秒级） |
| **计算量** | 很大 | 较小 |
| **输出** | 模型文件（.pt或.onnx） | 检测结果（边界框+类别） |
| **频率** | 偶尔进行（数据更新时） | 频繁进行（日常使用） |

### 1.4 四种类别说明

本项目需要识别荔枝的四个成熟度阶段：

1. **Immature（未成熟）**
   - 特征：果皮为绿色，果实较小
   - 应用：判断是否需要等待成熟

2. **Turing（转色期）**
   - 特征：果皮开始从绿色转为黄色或淡红色
   - 应用：判断即将进入成熟阶段

3. **Coloring（着色期）**
   - 特征：果皮明显变红，但可能不完全成熟
   - 应用：判断接近成熟，需要密切监测

4. **Ripe（成熟）**
   - 特征：果皮完全变红，达到最佳采摘时机
   - 应用：判断可以采摘

---

## 二、整体工作流程

### 2.1 完整流程图

```
┌─────────────────────────────────────────────────────────────┐
│                    1. 数据收集与标注                          │
│  - 使用相机拍摄荔枝图像                                       │
│  - 人工标注每张图像中荔枝的位置和成熟度类别                   │
│  - 生成YOLO格式的标注文件（.txt文件）                        │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    2. 数据集准备                              │
│  - 将图像和标注文件组织成训练集和验证集                       │
│  - 创建data.yaml配置文件（定义数据集路径和类别）              │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    3. 模型训练                                │
│  - 加载预训练模型（如yolov8n.pt）                            │
│  - 使用训练集训练模型，让模型学习荔枝成熟度特征               │
│  - 在验证集上评估模型性能                                     │
│  - 保存最佳模型（best.pt）                                   │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    4. 模型导出（可选）                        │
│  - 将PyTorch模型（.pt）转换为ONNX格式（.onnx）               │
│  - ONNX格式更便于部署，推理速度更快                           │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    5. 模型推理                                │
│  - 加载训练好的模型                                           │
│  - 输入新的荔枝图像                                           │
│  - 模型自动检测并识别所有荔枝的成熟度                         │
│  - 返回检测结果（位置、类别、置信度）                         │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 数据流向

```
原始图像 (JPG)
    ↓
人工标注 → YOLO格式标签文件 (TXT)
    ↓
数据集组织 (train/val)
    ↓
训练脚本 → 模型文件 (.pt)
    ↓
模型导出 → ONNX模型 (.onnx)
    ↓
推理脚本 → 检测结果 (JSON/图像)
```

---

## 三、训练流程详解

### 3.1 训练前的准备工作

#### 步骤0：原始数据来源与结构

本项目的数据集来源于 `e:\基于无人机与AI的荔枝产业应用\13_公开数据集\Datasets\Detection\Plain`，包含原始相机采集图像及初步分类数据。

**目录结构说明**：
- `LycheeCamera/collected/`：已按成熟度分类的图像整理目录
  - `Immature/`：未成熟期图像（如 `FT2526603_20250512170001.jpg`）
  - `Turning/`：转色期图像（如 `FT2526603_20250516101902.jpg`）
  - `Coloring/`：着色期图像（如 `FT2526603_20250610061901.jpg`）
  - `Ripe/`：成熟期图像（如 `FT2526603_20250625181901.jpg`）
- `LycheeCamera/FT2526603/`：采集设备 FT2526603 的原始图像（按日期归档，如 `20250509` 等）
- `LycheeCamera/FY4858258/`：采集设备 FY4858258 的原始图像（按日期归档，如 `20250511` 等）

这些原始数据经过筛选、标注和划分后，将被组织成标准的YOLO训练格式（如下文所示）。

#### 步骤1：数据集准备

**数据集结构**：
```
LycheeRipeDetection/
├── data.yaml              # 数据集配置文件
├── images/
│   ├── train/            # 训练集图像（295张）
│   │   ├── image1.jpg
│   │   ├── image2.jpg
│   │   └── ...
│   └── val/              # 验证集图像（101张）
│       ├── image1.jpg
│       └── ...
└── labels/
    ├── train/            # 训练集标签（295个txt文件）
    │   ├── image1.txt
    │   ├── image2.txt
    │   └── ...
    └── val/              # 验证集标签（101个txt文件）
        ├── image1.txt
        └── ...
```

**data.yaml文件内容**：
```yaml
train: images/train      # 训练集图像路径
val: images/val          # 验证集图像路径

names:                  # 类别名称（顺序很重要！）
  0: "Immature"         # 类别ID 0 → 未成熟
  1: "Turing"           # 类别ID 1 → 转色期
  2: "Coloring"         # 类别ID 2 → 着色期
  3: "Ripe"             # 类别ID 3 → 成熟
```

**标签文件格式（YOLO格式）**：
```
class_id x_center y_center width height
```

- 所有数值都是**归一化**的（0到1之间）
- `x_center`, `y_center`：边界框中心点的坐标（相对于图像宽高的比例）
- `width`, `height`：边界框的宽度和高度（相对于图像宽高的比例）

**示例**（`FY4858258_20250619061804.txt`）：
```
3 0.306640 0.573375 0.023168 0.057632
3 0.371841 0.564145 0.022954 0.040210
...
```

解释：
- `3`：类别ID（对应"Ripe"成熟荔枝）
- `0.306640`：边界框中心点x坐标（图像宽度的30.664%处）
- `0.573375`：边界框中心点y坐标（图像高度的57.3375%处）
- `0.023168`：边界框宽度（图像宽度的2.3168%）
- `0.057632`：边界框高度（图像高度的5.7632%）

#### 步骤2：安装必要的库

```bash
pip install ultralytics
pip install torch torchvision
```

### 3.2 训练代码详解

#### 文件位置
`yolov8/ultralytics-main/runPeriod_detection.py`

#### 完整代码
```python
from ultralytics import YOLO

# 方式1：从头开始构建模型（不常用）
# model = YOLO("CBAM-v8n.yaml")  

# 方式2：加载预训练模型（推荐！）
model = YOLO("yolov8n.pt")  

# 方式3：继续训练已有模型（如果有的话）
# model = YOLO("D:/yolov8/ultralytics-main/runs/detect/train6/weights/best.pt")

if __name__ == '__main__':
    # 开始训练
    model.train(
        data="lizhi-dt_period.yaml",  # 数据集配置文件路径
        epochs=300,                    # 训练轮数（把所有数据训练300遍）
        imgsz=640,                     # 输入图像尺寸（640x640像素）
        batch=16,                      # 批次大小（一次处理16张图像）
        patience=100                   # 早停耐心值（100轮没改进就停止）
    )  
    
    # 在验证集上评估模型性能
    metrics = model.val()
    
    # 查看评估结果
    print(metrics)
```

#### 逐行详细解释

**第1行：导入YOLO类**
```python
from ultralytics import YOLO
```
- `ultralytics` 是一个流行的目标检测库
- `YOLO` 是核心类，用于加载、训练和使用模型
- **作用**：引入必要的工具

**第4-7行：加载模型**
```python
# 方式1：从头开始构建模型
# model = YOLO("CBAM-v8n.yaml")  

# 方式2：加载预训练模型（推荐）
model = YOLO("yolov8n.pt")  
```

**什么是预训练模型？**
- 就像有一个已经学过识别很多物体的"通才学生"
- 这个学生已经知道识别物体的基本方法（如边缘、纹理等）
- 现在只需要教他识别荔枝成熟度（比从头教更高效）

**三种加载方式对比**：

| 方式 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| 方式1（YAML） | 完全自定义 | 训练时间长，需要大量数据 | 研究新架构 |
| 方式2（.pt预训练） | **训练快，效果好** | 需要下载预训练权重 | **大多数情况（推荐）** |
| 方式3（已有模型） | 在已有基础上改进 | 需要有已训练的模型 | 继续训练/微调 |

**第11-16行：训练模型**
```python
model.train(
    data="lizhi-dt_period.yaml",  # 数据集配置文件
    epochs=300,                    # 训练轮数
    imgsz=640,                     # 图像尺寸
    batch=16,                      # 批次大小
    patience=100                   # 早停耐心值
)
```

**参数详解**：

1. **`data="lizhi-dt_period.yaml"`**
   - **作用**：告诉模型数据集在哪里，有哪些类别
   - **内容**：包含训练集路径、验证集路径、类别名称
   - **注意**：对于成熟度检测，应该使用 `LycheeRipeDetection/data.yaml`

2. **`epochs=300`**
   - **作用**：训练多少轮
   - **解释**：一个epoch = 把训练集所有图像训练一遍
   - **300轮** = 把所有图像训练300遍
   - **为什么需要多轮**：模型需要反复学习才能掌握特征
   - **类比**：学生需要反复练习才能熟练掌握知识

3. **`imgsz=640`**
   - **作用**：输入图像的尺寸（宽x高）
   - **640** = 640像素 × 640像素
   - **为什么统一尺寸**：模型需要固定大小的输入
   - **如何处理不同尺寸图像**：自动缩放和填充（保持宽高比）

4. **`batch=16`**
   - **作用**：一次处理多少张图像
   - **16** = 一次处理16张图像，然后更新模型参数
   - **为什么分批**：
     - 内存限制（不能一次加载所有图像）
     - 提高训练效率（并行处理）
   - **如何选择**：
     - 内存大 → batch可以更大（如32）
     - 内存小 → batch需要更小（如8）

5. **`patience=100`**
   - **作用**：早停机制
   - **解释**：如果连续100轮模型性能没有提升，自动停止训练
   - **为什么需要**：
     - 防止过度训练（过拟合）
     - 节省时间和计算资源
   - **类比**：如果学生100次考试都没进步，说明已经学到极限了

**训练过程发生了什么？**

```
开始训练
    ↓
第1轮（Epoch 1）：
  - 加载16张图像 → 模型预测 → 计算误差 → 调整参数
  - 重复直到所有图像训练完
  - 在验证集上测试性能（如准确率50%）
    ↓
第2轮（Epoch 2）：
  - 用调整后的参数再次训练
  - 验证集性能可能提升（如准确率60%）
    ↓
...
    ↓
第150轮（Epoch 150）：
  - 验证集性能达到85%
  - 模型学会了识别大部分荔枝成熟度
    ↓
...
    ↓
第300轮或达到patience限制：
  - 训练结束
  - 保存最佳模型（best.pt）
```

**第19行：验证模型**
```python
metrics = model.val()
```
- **作用**：在验证集上评估模型性能
- **返回结果**：
  - `mAP50`：平均精度（主要指标，越高越好，0-1之间）
  - `mAP50-95`：更严格的精度指标
  - `precision`：精确率（预测为成熟，实际有多少真的是成熟）
  - `recall`：召回率（实际是成熟，模型找到了多少）

### 3.3 训练输出文件

训练完成后，会在 `runs/detect/trainX/` 目录下生成：

```
runs/detect/train6/
├── weights/
│   ├── best.pt          # 验证集上表现最好的模型（用于推理）
│   └── last.pt          # 最后一轮的模型
├── results.png          # 训练过程可视化（损失、准确率曲线）
├── confusion_matrix.png # 混淆矩阵（各类别识别情况）
└── ...
```

**best.pt vs last.pt**：
- `best.pt`：**推荐使用**，验证集上性能最好的模型
- `last.pt`：最后一轮训练的模型（可能不如best.pt）

---

## 四、推理流程详解

### 4.1 推理前的准备工作

#### 步骤1：准备模型文件

需要训练好的模型文件：
- PyTorch格式：`best.pt`
- ONNX格式：`v5.onnx`（通过导出得到，可选）

#### 步骤2：准备待检测图像

可以是：
- 单张图像：`image.jpg`
- 图像文件夹：`images/`
- Base64编码的字符串（用于API）

### 4.2 推理代码详解

#### 文件位置
`LycheeDetection-master/LycheeDetection-master/detect.py`

#### 核心类：Detector

这个类封装了完整的推理流程，让我们详细看看每个部分：

---

### 4.3 初始化函数：`__init__()`

```python
def __init__(
    self,
    model_path: str,              # ONNX模型文件路径
    class_names: list[str] = CLASS_NAMES,  # 类别名称列表
    conf_threshold: float = 0.5,  # 置信度阈值
    iou_threshold: float = 0.5,   # IoU阈值（用于NMS）
    save_results: bool = False,   # 是否保存结果
    max_save: int = 20,           # 最多保存多少个结果
):
```

**详细解释**：

1. **`model_path`**：
   - 训练好的ONNX模型文件路径
   - 例如：`"models/v5.onnx"`

2. **`class_names`**：
   - 类别名称列表
   - 默认：`["Immature", "Turing", "Coloring", "Ripe"]`
   - **顺序很重要**！必须和训练时的顺序一致

3. **`conf_threshold=0.5`**：
   - 置信度阈值
   - **作用**：过滤掉置信度低的检测结果
   - **解释**：如果模型预测某个荔枝是"成熟"，但置信度只有30%，可能不可靠，会被过滤
   - **0.5** = 只保留置信度≥50%的检测结果
   - **调整**：
     - 提高（如0.7）→ 更严格，只保留很确信的结果，但可能漏掉一些
     - 降低（如0.3）→ 更宽松，保留更多结果，但可能有误检

4. **`iou_threshold=0.5`**：
   - IoU（交并比）阈值，用于NMS（非极大值抑制）
   - **作用**：去除重复的检测框
   - **解释**：如果一个荔枝被检测到多次（多个重叠的框），只保留最好的一个
   - **IoU**：两个框的重叠程度（0-1之间）
   - **0.5** = 如果两个框重叠超过50%，去掉置信度低的那个

5. **`save_results`**：
   - 是否保存调试结果
   - 如果为True，会保存原始图像、标注图像和JSON结果

---

### 4.4 图像加载函数：`load_image()`

```python
def load_image(self, image: str) -> np.ndarray:
    """加载图像，支持文件路径或base64编码"""
```

**功能**：
- 从文件路径加载图像
- 或从base64编码字符串解码图像
- 转换为RGB格式的numpy数组

**使用示例**：
```python
# 方式1：文件路径
image = detector.load_image("lychee.jpg")

# 方式2：base64编码（用于API）
base64_str = "data:image/jpeg;base64,/9j/4AAQSkZJRg..."
image = detector.load_image(base64_str)
```

---

### 4.5 图像预处理函数：`letterbox()`

```python
def letterbox(
    self,
    img: np.ndarray,
    new_shape: int | tuple[int, int] = 640,
    color: tuple[int, int, int] = (114, 114, 114),
    scaleup: bool = True,
) -> tuple[np.ndarray, float, tuple[float, float]]:
```

**功能**：将图像调整为固定尺寸，同时保持宽高比

**为什么需要？**
- 模型的输入必须是固定尺寸（如640x640）
- 但实际图像的尺寸可能各不相同（1920x1080、800x600等）
- 需要统一尺寸，但又要保持图像不变形

**letterbox的原理**：

```
原始图像：800 x 600
目标尺寸：640 x 640

步骤1：计算缩放比例
  - 按宽度缩放：640/800 = 0.8
  - 按高度缩放：640/600 = 1.067
  - 取较小的：0.8（保证图像完全放入，不裁剪）

步骤2：缩放图像
  - 新尺寸：800*0.8 x 600*0.8 = 640 x 480

步骤3：填充空白
  - 需要的高度：640 - 480 = 160
  - 上下各填充：80像素（灰色填充）
  - 最终：640 x 640（上下有灰色边框）
```

**返回值**：
- `img`：调整后的图像
- `r`：缩放比例（用于后续坐标转换）
- `(dw, dh)`：填充的宽度和高度（用于后续坐标转换）

---

### 4.6 完整预处理函数：`preprocess()`

```python
def preprocess(self, image: np.ndarray) -> tuple[np.ndarray, float, tuple[float, float]]:
    img, r, (dw, dh) = self.letterbox(image, new_shape=self.input_shape)
    img = img.astype(np.float32) / 255.0  # 归一化到[0,1]
    img = np.transpose(img, (2, 0, 1))    # HWC → CHW
    img = np.expand_dims(img, axis=0)     # 添加batch维度
    return np.ascontiguousarray(img), r, (dw, dh)
```

**功能**：完整的预处理流程

**步骤详解**：

1. **`letterbox()`**：调整尺寸
   - 输入：任意尺寸的图像
   - 输出：640x640的图像

2. **归一化**：`img / 255.0`
   - 将像素值从0-255转换为0-1
   - **为什么**：深度学习模型通常使用0-1范围的输入，训练更快更稳定

3. **维度转换**：`np.transpose(img, (2, 0, 1))`
   - 从 `(H, W, C)` → `(C, H, W)`
   - H=高度，W=宽度，C=通道数（RGB=3）
   - **为什么**：PyTorch/ONNX使用CHW格式
   - **示例**：
     ```
     原始：(640, 640, 3)  # 高度、宽度、通道
     转换：(3, 640, 640)  # 通道、高度、宽度
     ```

4. **添加batch维度**：`np.expand_dims(img, axis=0)`
   - 从 `(C, H, W)` → `(1, C, H, W)`
   - **为什么**：模型需要batch维度（即使只有一张图像）
   - **示例**：
     ```
     转换前：(3, 640, 640)
     转换后：(1, 3, 640, 640)  # 1张图像，3个通道，640x640
     ```

5. **`np.ascontiguousarray()`**：
   - 确保数组在内存中是连续存储的
   - **为什么**：提高后续操作的速度

---

### 4.7 模型推理：ONNX Runtime

```python
inputs = {self._input_name: processed_image}
outputs = self.session.run([self._output_name], inputs)
```

**功能**：运行ONNX模型推理

**解释**：
- `self.session`：ONNX Runtime会话（已加载的模型）
- `inputs`：模型的输入（预处理后的图像）
- `outputs`：模型的输出（原始检测结果）

**模型输出格式**：
```
形状：(1, 8400, 8)
- 1：batch大小（1张图像）
- 8400：可能的检测框数量（不同尺度、不同位置）
- 8：每个检测框的信息
  - 前4个：边界框坐标（x_center, y_center, width, height）
  - 后4个：4个类别的置信度分数（Immature, Turing, Coloring, Ripe）
```

**示例输出**：
```
检测框1：[0.3, 0.5, 0.02, 0.05, 0.1, 0.2, 0.1, 0.8]
         ↑    ↑    ↑    ↑    ↑    ↑    ↑    ↑
         坐标  坐标  宽   高   Imm  Tur  Col  Rip
                                    ↑最高，说明是"成熟"
```

---

### 4.8 后处理函数：`postprocess_output()`

这是最复杂的部分，让我们逐步理解：

#### 步骤1：解析模型输出

```python
pred = np.squeeze(output).T
boxes = pred[:, :4]          # 前4列：边界框坐标
class_scores = pred[:, 4:]   # 后4列：类别分数
confidences = np.amax(class_scores, axis=1)  # 每个框的最大置信度
class_ids = np.argmax(class_scores, axis=1)  # 每个框的类别ID
```

**解释**：
- `pred`：模型原始输出（8400个检测框）
- `boxes`：所有检测框的坐标
- `class_scores`：每个检测框对4个类别的置信度分数
- `confidences`：每个检测框的最高置信度
- `class_ids`：每个检测框最可能的类别（0/1/2/3）

#### 步骤2：置信度过滤

```python
mask = confidences >= self.conf_threshold
boxes = boxes[mask]
confidences = confidences[mask]
class_ids = class_ids[mask]
```

**功能**：只保留置信度≥阈值的检测结果

**示例**：
```
过滤前：1000个检测框
- 框1：置信度0.9 → 保留 ✓
- 框2：置信度0.3 → 过滤 ✗
- 框3：置信度0.7 → 保留 ✓
过滤后：500个检测框（只保留置信度≥0.5的）
```

#### 步骤3：坐标格式转换

```python
# 从中心格式 (cxcywh) 转换为角点格式 (xyxy)
x_center, y_center, width, height = boxes[:, 0], boxes[:, 1], boxes[:, 2], boxes[:, 3]
x1 = x_center - width / 2
y1 = y_center - height / 2
x2 = x_center + width / 2
y2 = y_center + height / 2
```

**为什么需要转换？**
- 模型输出：中心点坐标 + 宽高 `(cx, cy, w, h)`
- 绘制需要：左上角 + 右下角 `(x1, y1, x2, y2)`

**示例**：
```
中心格式：(0.5, 0.5, 0.1, 0.1)
          ↑    ↑    ↑   ↑
         中心x 中心y 宽  高

转换后：(0.45, 0.45, 0.55, 0.55)
        ↑     ↑     ↑     ↑
       左上x  左上y  右下x  右下y
```

#### 步骤4：坐标还原到原始图像

```python
boxes[:, [0, 2]] = (boxes[:, [0, 2]] - dw) / r  # x坐标
boxes[:, [1, 3]] = (boxes[:, [1, 3]] - dh) / r  # y坐标
```

**为什么需要还原？**
- 模型处理的是640x640的图像（可能被缩放和填充过）
- 但结果需要在原始图像上显示
- 需要反向计算原始坐标

**示例**：
```
预处理时：
  原始图像：1920x1080
  缩放比例r = 0.333 (640/1920)
  填充：左右各填充160像素

模型输出：
  x = 0.5 (在640x640图像中的位置)

还原计算：
  x_original = (0.5 - 160) / 0.333
             = 1020 (在1920宽度的原始图像中的位置)
```

#### 步骤5：裁剪边界框

```python
boxes[:, [0, 2]] = np.clip(boxes[:, [0, 2]], 0, w)
boxes[:, [1, 3]] = np.clip(boxes[:, [1, 3]], 0, h)
```

**功能**：确保边界框在图像范围内

**为什么需要？**
- 有时候边界框可能超出图像边界（负值或超过宽度/高度）
- 需要裁剪到有效范围

#### 步骤6：非极大值抑制（NMS）

```python
indices = cv2.dnn.NMSBoxes(
    bboxes=boxes_xywh.tolist(),
    scores=confidences.tolist(),
    score_threshold=self.conf_threshold,
    nms_threshold=self.iou_threshold,
)
```

**功能**：去除重复的检测框

**什么是NMS？**
- 同一个荔枝可能被检测到多次（产生多个重叠的框）
- NMS会保留置信度最高的，删除其他重叠的框

**NMS原理**：
```
检测结果（NMS前）：
  框1：[x=100, y=100, w=50, h=50], 置信度=0.9, 类别="成熟"
  框2：[x=105, y=105, w=48, h=48], 置信度=0.7, 类别="成熟"  ← 与框1重叠
  框3：[x=300, y=200, w=45, h=45], 置信度=0.8, 类别="成熟"

NMS过程：
  1. 按置信度排序：框1(0.9) > 框3(0.8) > 框2(0.7)
  2. 保留框1（置信度最高）
  3. 计算框1和框2的IoU = 0.8（重叠80%）
     - IoU > 阈值(0.5) → 删除框2（置信度低）
  4. 计算框1和框3的IoU = 0.1（不重叠）
     - IoU < 阈值(0.5) → 保留框3

结果（NMS后）：
  框1和框3被保留，框2被删除
```

**返回结果**：
```python
return {
    "boxes": [[x1, y1, x2, y2], ...],           # 边界框列表
    "confidences": ["0.95", "0.87", ...],       # 置信度列表
    "class_names": ["Ripe", "Coloring", ...]    # 类别名称列表
}
```

---

### 4.9 主检测函数：`detect()`

```python
def detect(self, image: str | np.ndarray) -> dict:
    original_image = self.load_image(image) if isinstance(image, str) else image
    processed_image, r, (dw, dh) = self.preprocess(original_image)
    inputs = {self._input_name: processed_image}
    outputs = self.session.run([self._output_name], inputs)
    detections = self.postprocess_output(
        outputs[0], original_shape=original_image.shape[:2], r=r, dw=dw, dh=dh
    )
    if self.save_results:
        self.save_detections(original_image, detections)
    return detections
```

**功能**：完整的检测流程

**步骤**：
1. 加载图像
2. 预处理
3. 模型推理
4. 后处理
5. 保存结果（可选）
6. 返回结果

---

### 4.10 结果可视化：`plot_detections()`

```python
def plot_detections(self, image: np.ndarray, detections: dict) -> np.ndarray:
    for box, conf, class_name in zip(
        detections["boxes"], detections["confidences"], detections["class_names"]
    ):
        x1, y1, x2, y2 = map(int, box)
        label = f"{class_name}: {conf}"
        cv2.rectangle(image, (x1, y1), (x2, y2), (255, 0, 0), 2)
        cv2.putText(image, label, (x1, y1 - 5), cv2.FONT_HERSHEY_SIMPLEX, 0.5, (255, 0, 0), 2)
    return image
```

**功能**：在图像上绘制检测结果

**绘制内容**：
- 边界框：蓝色矩形框
- 标签：类别名称 + 置信度（如"Ripe: 0.95"）

---

## 五、完整使用示例

### 5.1 训练示例

```python
from ultralytics import YOLO

# 1. 加载预训练模型
model = YOLO("yolov8n.pt")

# 2. 开始训练
model.train(
    data="13_公开数据集/Datasets/Detection/LycheeRipeDetection/data.yaml",
    epochs=300,
    imgsz=640,
    batch=16,
    patience=100,
    name="lychee_ripe_detection"  # 训练结果的文件夹名称
)

# 3. 验证模型
metrics = model.val()
print(f"mAP50: {metrics.box.map50}")  # 打印平均精度
```

**运行后**：
- 会在 `runs/detect/lychee_ripe_detection/weights/` 生成 `best.pt`

### 5.2 推理示例（使用PyTorch模型）

```python
from ultralytics import YOLO

# 1. 加载训练好的模型
model = YOLO("runs/detect/lychee_ripe_detection/weights/best.pt")

# 2. 对单张图像进行预测
results = model.predict(
    source="lychee_image.jpg",
    save=True,           # 保存结果图像
    conf=0.5,            # 置信度阈值
    show=False           # 不显示图像（如果是在服务器上）
)

# 3. 查看结果
for result in results:
    boxes = result.boxes
    for box in boxes:
        class_id = int(box.cls[0])
        confidence = float(box.conf[0])
        class_name = result.names[class_id]
        x1, y1, x2, y2 = box.xyxy[0].tolist()
        
        print(f"检测到: {class_name}")
        print(f"  置信度: {confidence:.2%}")
        print(f"  位置: ({x1:.0f}, {y1:.0f}, {x2:.0f}, {y2:.0f})")
```

### 5.3 推理示例（使用ONNX模型）

```python
from detect import Detector

# 1. 初始化检测器
detector = Detector(
    model_path="models/v5.onnx",
    conf_threshold=0.5,
    iou_threshold=0.5,
    save_results=True
)

# 2. 检测单张图像
detections = detector.detect("lychee_image.jpg")

# 3. 查看结果
print(f"检测到 {len(detections['boxes'])} 个荔枝")
for i, (box, conf, class_name) in enumerate(zip(
    detections["boxes"],
    detections["confidences"],
    detections["class_names"]
)):
    print(f"\n荔枝 {i+1}:")
    print(f"  类别: {class_name}")
    print(f"  置信度: {conf}")
    print(f"  位置: {box}")
```

### 5.4 API服务示例

```python
# 启动API服务
# python main.py

# 使用curl测试
curl -X POST "http://localhost:8000/detect/" \
  -H "Content-Type: application/json" \
  -d '{"image": "base64_encoded_image_string"}'
```

---

## 六、常见问题解答

### Q1: 训练需要多长时间？

**答案**：取决于：
- **数据集大小**：图像越多，时间越长
- **模型大小**：yolov8n最快，yolov8x最慢
- **硬件配置**：
  - CPU：可能需要几小时到几天
  - GPU（NVIDIA）：通常几分钟到几小时
- **训练轮数**：epochs越多，时间越长

**建议**：
- 先用小数据集和少量epoch测试（如10个epoch）
- 确认流程正确后，再完整训练

### Q2: 如何判断模型训练好了？

**指标**：
1. **mAP50**：应该>0.8（越高越好）
2. **loss曲线**：应该逐渐下降并趋于平稳
3. **验证集准确率**：应该接近训练集（差距太大说明过拟合）

**检查方法**：
```python
# 查看训练结果
metrics = model.val()
print(f"mAP50: {metrics.box.map50}")
print(f"mAP50-95: {metrics.box.map}")
```

### Q3: 置信度阈值如何设置？

**建议**：
- **开发阶段**：0.3-0.4（保留更多结果，方便调试）
- **生产环境**：0.5-0.7（只保留高置信度结果，减少误检）
- **严格场景**：0.7-0.9（只保留非常确信的结果）

**调整方法**：
```python
detector = Detector(
    model_path="models/v5.onnx",
    conf_threshold=0.6  # 调整这里
)
```

### Q4: 如何提高检测准确率？

**方法**：
1. **增加训练数据**：更多、更多样化的图像
2. **数据增强**：旋转、缩放、亮度调整等
3. **调整超参数**：learning_rate、batch_size等
4. **使用更大的模型**：yolov8s、yolov8m、yolov8l
5. **微调**：在预训练模型基础上继续训练

### Q5: 推理速度慢怎么办？

**优化方法**：
1. **使用ONNX格式**：比PyTorch快
2. **降低图像尺寸**：imgsz从640降到416
3. **使用GPU**：比CPU快很多
4. **批次处理**：一次处理多张图像
5. **模型量化**：使用INT8量化模型

### Q6: 检测结果不准确？

**可能原因**：
1. **训练数据不足**：增加更多训练图像
2. **标注错误**：检查标注文件是否正确
3. **类别不平衡**：某些类别样本太少
4. **模型未充分训练**：增加epochs

**解决方法**：
- 检查混淆矩阵（confusion_matrix.png）
- 查看哪些类别识别不好
- 针对性地增加该类别的训练数据

### Q7: 如何部署到生产环境？

**步骤**：
1. **导出ONNX模型**（更快、更易部署）
2. **使用API服务**（如FastAPI）
3. **容器化**（Docker）
4. **负载均衡**（处理大量请求）

---

## 七、术语表

| 术语 | 英文 | 解释 |
|------|------|------|
| 训练 | Training | 让模型从数据中学习的过程 |
| 推理 | Inference | 使用训练好的模型进行预测 |
| 批次 | Batch | 一次处理的图像数量 |
| 轮数 | Epoch | 把所有训练数据训练一遍 |
| 置信度 | Confidence | 模型对预测结果的把握程度 |
| 边界框 | Bounding Box | 标记目标位置的矩形框 |
| 标注 | Annotation | 人工标注的图像标签 |
| 验证集 | Validation Set | 用于评估模型性能的数据集 |
| 损失 | Loss | 模型预测与真实值的差距 |
| mAP | Mean Average Precision | 平均精度均值，评价指标 |
| NMS | Non-Maximum Suppression | 非极大值抑制，去除重复检测 |
| IoU | Intersection over Union | 交并比，衡量两个框的重叠程度 |
| ONNX | Open Neural Network Exchange | 跨平台模型格式 |

---

## 八、扩展阅读

### 8.1 进一步学习

1. **YOLO原理**：
   - YOLO v8官方文档：https://docs.ultralytics.com/
   - YOLO论文：You Only Look Once

2. **深度学习基础**：
   - 卷积神经网络（CNN）
   - 目标检测基础概念

3. **数据标注工具**：
   - LabelImg：图像标注工具
   - Roboflow：在线数据标注平台

### 8.2 相关文件位置

```
项目结构：
├── 13_公开数据集/
│   └── Datasets/
│       └── Detection/
│           └── LycheeRipeDetection/    # 数据集
│               ├── data.yaml
│               ├── images/
│               └── labels/
│
├── yolov8/
│   └── ultralytics-main/
│       ├── runPeriod_detection.py      # 训练脚本
│       └── Predict_Period.py           # 推理脚本（PyTorch）
│
└── LycheeDetection-master/
    └── LycheeDetection-master/
        ├── detect.py                   # 推理脚本（ONNX）
        ├── main.py                     # API服务
        └── lifespan.py                 # 模型加载
```

---

## 九、总结

### 训练流程（一次性工作）
1. 准备数据集和标注
2. 配置data.yaml
3. 运行训练脚本
4. 等待训练完成
5. 得到best.pt模型文件

### 推理流程（日常使用）
1. 加载训练好的模型
2. 输入新图像
3. 模型自动检测
4. 获得检测结果（位置+类别+置信度）

### 关键要点
- **训练**：让模型学习知识（需要大量时间和计算资源）
- **推理**：使用模型解决问题（快速、实时）
- **数据质量**：高质量的数据集是成功的关键
- **参数调整**：根据实际情况调整阈值和超参数

---

## 十、实际应用场景与扩展

### 10.1 成熟度检测 vs 物候期识别

在荔枝产业应用中，有两个重要的识别任务，它们有不同的目标和应用场景：

#### 成熟度检测（LycheeRipeDetection）

**定义**：识别荔枝果实的成熟度阶段

**类别**：
- **Immature（未成熟）**：果皮为绿色，果实较小
- **Turing（转色期）**：果皮开始从绿色转为黄色或淡红色
- **Coloring（着色期）**：果皮明显变红
- **Ripe（成熟）**：果皮完全变红，可以采摘

**标注对象**：单个荔枝果实

**应用时间**：结果期（5-7月）

**应用场景**：
- 判断何时采摘荔枝
- 自动采摘机器人
- 果实分级筛选

**数据集位置**：`13_公开数据集/Datasets/Detection/LycheeRipeDetection/`

#### 物候期识别（需要新建）

**定义**：识别荔枝植株的生长阶段

**类别示例**（根据你的需求）：
- **QiuShao（秋梢期）**：秋季新梢生长期
- **LuBaiDian（白点期）**：花芽分化期（花芽出现白色小点）

**标注对象**：整棵荔枝树或枝条（不是果实）

**应用时间**：整个生长周期（9月-次年3月）

**应用场景**：
- 判断植株生长阶段
- 指导农事管理（施肥、修剪等）
- 预测开花、结果时间

**数据集位置**：需要新建 `LycheePhenologicalDetection/`

#### 对比表

| 特征 | 成熟度检测（LycheeRipeDetection） | 物候期识别（需要新建） |
|------|----------------------------------|----------------------|
| **对象** | 果实（荔枝果子） | 植株（整棵荔枝树或枝条） |
| **阶段** | 4个成熟度阶段（未成熟→成熟） | 多个物候期阶段（秋梢→白点→...） |
| **时间** | 结果期（5-7月） | 整个生长周期（9月-次年3月） |
| **应用** | 判断何时采摘 | 判断生长阶段，指导农事管理 |
| **标注内容** | 标注每个果实的位置和成熟度 | 标注树或枝条的位置和物候期阶段 |

### 10.2 如何创建物候期识别数据集

#### 场景示例

**时间**：2026年1月20日（大寒节气）  
**品种**：仙进奉荔枝  
**当前阶段**：秋梢期与白点期之间  
**数据来源**：荔枝园低空监控摄像头拍摄的影像

#### 步骤1：创建数据集文件夹结构

```bash
# 在 Detection 目录下新建物候期检测数据集文件夹
mkdir -p 13_公开数据集/Datasets/Detection/LycheePhenologicalDetection/{images,labels}/{train,val}

# 或者手动创建以下结构：
```

**文件夹结构**：
```
13_公开数据集/Datasets/Detection/
├── LycheeRipeDetection/              # 成熟度检测（已有，不修改）
│   └── ...
│
└── LycheePhenologicalDetection/      # 物候期检测（新建）
    ├── data.yaml                     # 配置文件
    ├── images/
    │   ├── train/                    # 训练集图像
    │   └── val/                      # 验证集图像
    └── labels/
        ├── train/                    # 训练集标签（YOLO格式）
        └── val/                      # 验证集标签（YOLO格式）
```

#### 步骤2：创建 data.yaml 配置文件

在 `LycheePhenologicalDetection/` 目录下创建 `data.yaml` 文件：

```yaml
# LycheePhenologicalDetection/data.yaml
train: images/train      # 训练集图像路径
val: images/val          # 验证集图像路径

names:                  # 类别名称（顺序很重要！）
  0: "QiuShao"         # 类别ID 0 → 秋梢期
  1: "LuBaiDian"       # 类别ID 1 → 白点期
```

**注意**：根据你的实际需求，可以添加更多物候期阶段：
```yaml
names:
  0: "QiuShao"         # 秋梢期
  1: "LuBaiDian"       # 白点期
  2: "HuaYa"           # 花芽分化期
  3: "KaiHua"          # 开花期
  # ... 其他阶段
```

#### 步骤3：准备图像数据

1. **收集图像**：
   - 将低空监控摄像头拍摄的影像保存到 `images/train/` 或 `images/val/`
   - 建议图像格式：JPG、JPEG、PNG

2. **数据集划分**：
   - 将图像按 **80:20** 的比例划分为训练集和验证集
   - 训练集：80%（用于训练模型）
   - 验证集：20%（用于评估模型性能）

3. **命名规范**：
   - 建议使用有意义的文件名，如：`20260120_001.jpg`（日期_序号）
   - 图像文件名和标签文件名要对应（如 `image1.jpg` 对应 `image1.txt`）

#### 步骤4：标注数据（YOLO格式）

##### 4.1 标注工具

**推荐工具**：
- **LabelImg**：开源标注工具，支持YOLO格式
  - 下载地址：https://github.com/HumanSignal/labelImg
  - 使用方法：打开工具 → 选择Open Dir（打开图像文件夹）→ 选择YOLO格式 → 标注

- **Roboflow**：在线标注平台
  - 网址：https://roboflow.com/
  - 支持在线标注和数据集管理

##### 4.2 标注注意事项

**标注对象**：
- ✅ **标注整棵荔枝树**：如果图像中有完整的荔枝树
- ✅ **标注枝条**：如果只看到部分枝条
- ❌ **不要标注果实**：物候期识别关注的是植株生长阶段，不是果实

**标注原则**：
1. **边界框要完整**：框住整个识别对象（树或枝条）
2. **边界框要紧凑**：尽量不要包含太多背景
3. **标注要准确**：确保类别标签正确（0=秋梢期，1=白点期）

##### 4.3 YOLO格式标签文件

每个图像对应一个同名的 `.txt` 标签文件。

**标签格式**：
```
class_id x_center y_center width height
```

- 所有数值都是**归一化的**（0到1之间）
- `class_id`：类别ID（0=秋梢期，1=白点期）
- `x_center, y_center`：边界框中心点的坐标（相对于图像宽高的比例）
- `width, height`：边界框的宽度和高度（相对于图像宽高的比例）

**示例**（`20260120_001.txt`）：
```
0 0.5 0.5 0.3 0.4
1 0.8 0.3 0.15 0.2
```

解释：
- 第1行：类别0（秋梢期），中心点(0.5, 0.5)，宽度0.3，高度0.4
- 第2行：类别1（白点期），中心点(0.8, 0.3)，宽度0.15，高度0.2

##### 4.4 标签文件命名规则

标签文件必须与图像文件同名，扩展名为 `.txt`：

```
images/train/20260120_001.jpg  →  labels/train/20260120_001.txt
images/train/20260120_002.jpg  →  labels/train/20260120_002.txt
```

#### 步骤5：数据集划分脚本（可选）

如果你需要自动划分数据集，可以使用以下脚本：

```python
# split_dataset.py
import os
import random
import shutil

# 配置路径
source_images = "images_all/"  # 所有图像的源文件夹
source_labels = "labels_all/"  # 所有标签的源文件夹

train_images = "images/train/"
val_images = "images/val/"
train_labels = "labels/train/"
val_labels = "labels/val/"

# 创建目标文件夹
os.makedirs(train_images, exist_ok=True)
os.makedirs(val_images, exist_ok=True)
os.makedirs(train_labels, exist_ok=True)
os.makedirs(val_labels, exist_ok=True)

# 获取所有图像文件名
images = [f for f in os.listdir(source_images) if f.lower().endswith(('.jpg', '.jpeg', '.png'))]

# 随机打乱
random.shuffle(images)

# 按80:20划分
split_idx = int(len(images) * 0.8)
train_list = images[:split_idx]
val_list = images[split_idx:]

# 复制训练集
for img in train_list:
    # 复制图像
    shutil.copy(os.path.join(source_images, img), os.path.join(train_images, img))
    # 复制标签（如果存在）
    label_name = os.path.splitext(img)[0] + '.txt'
    if os.path.exists(os.path.join(source_labels, label_name)):
        shutil.copy(os.path.join(source_labels, label_name), os.path.join(train_labels, label_name))

# 复制验证集
for img in val_list:
    # 复制图像
    shutil.copy(os.path.join(source_images, img), os.path.join(val_images, img))
    # 复制标签（如果存在）
    label_name = os.path.splitext(img)[0] + '.txt'
    if os.path.exists(os.path.join(source_labels, label_name)):
        shutil.copy(os.path.join(source_labels, label_name), os.path.join(val_labels, label_name))

print(f"数据集划分完成！")
print(f"训练集：{len(train_list)} 张图像")
print(f"验证集：{len(val_list)} 张图像")
```

### 10.3 训练物候期识别模型

数据集准备好后，就可以开始训练模型了。

#### 步骤1：准备训练脚本

创建训练脚本 `train_phenological.py`：

```python
from ultralytics import YOLO

# 加载预训练模型
model = YOLO("yolov8n.pt")  # 也可以使用 yolov8s.pt, yolov8m.pt 等

if __name__ == '__main__':
    # 开始训练物候期识别模型
    model.train(
        data="13_公开数据集/Datasets/Detection/LycheePhenologicalDetection/data.yaml",  # 数据集配置文件
        epochs=300,                    # 训练轮数
        imgsz=640,                     # 输入图像尺寸（640x640像素）
        batch=16,                      # 批次大小（一次处理16张图像）
        patience=100,                  # 早停耐心值（100轮没改进就停止）
        name="lychee_phenological_detection"  # 训练结果文件夹名称
    )
    
    # 在验证集上评估模型性能
    metrics = model.val()
    print(f"mAP50: {metrics.box.map50}")  # 打印平均精度
    print(f"训练完成！模型保存在 runs/detect/lychee_phenological_detection/weights/best.pt")
```

#### 步骤2：运行训练

```bash
python train_phenological.py
```

#### 步骤3：训练输出

训练完成后，模型会保存在：
```
yolov8/ultralytics-main/runs/detect/lychee_phenological_detection/
├── weights/
│   ├── best.pt          # 验证集上表现最好的模型（用于推理）
│   └── last.pt          # 最后一轮的模型
├── results.png          # 训练过程可视化（损失、准确率曲线）
├── confusion_matrix.png # 混淆矩阵（各类别识别情况）
└── ...
```

### 10.4 使用训练好的模型进行推理

训练完成后，可以使用模型对新图像进行预测。

#### 推理代码示例

```python
from ultralytics import YOLO

# 1. 加载训练好的物候期识别模型
model = YOLO("runs/detect/lychee_phenological_detection/weights/best.pt")

# 2. 对单张图像进行预测
results = model.predict(
    source="your_image.jpg",           # 待检测图像路径
    save=True,                          # 保存结果图像
    conf=0.5,                           # 置信度阈值
    show=False                          # 不显示图像（如果是在服务器上）
)

# 3. 查看结果
for result in results:
    boxes = result.boxes
    print(f"检测到 {len(boxes)} 个目标")
    
    for box in boxes:
        class_id = int(box.cls[0])
        confidence = float(box.conf[0])
        class_name = result.names[class_id]  # 获取类别名称
        x1, y1, x2, y2 = box.xyxy[0].tolist()  # 获取边界框坐标
        
        print(f"\n检测到: {class_name}")
        print(f"  置信度: {confidence:.2%}")
        print(f"  位置: ({x1:.0f}, {y1:.0f}, {x2:.0f}, {y2:.0f})")
```

### 10.5 成熟度检测模型的训练代码

如果你要训练成熟度检测模型（使用现有的 `LycheeRipeDetection` 数据集），可以参考以下代码：

```python
from ultralytics import YOLO

# 加载预训练模型
model = YOLO("yolov8n.pt")

if __name__ == '__main__':
    # 开始训练荔枝成熟度检测模型
    model.train(
        data="13_公开数据集/Datasets/Detection/LycheeRipeDetection/data.yaml",  # 成熟度检测数据集配置
        epochs=300,                    # 训练轮数
        imgsz=640,                     # 输入图像尺寸
        batch=16,                      # 批次大小
        patience=100,                  # 早停耐心值
        name="lychee_ripe_detection"   # 训练结果文件夹名称
    )
    
    # 在验证集上评估模型性能
    metrics = model.val()
    print(f"mAP50: {metrics.box.map50}")
    print(f"训练完成！模型保存在 runs/detect/lychee_ripe_detection/weights/best.pt")
```

**训练后的模型位置**：
```
yolov8/ultralytics-main/runs/detect/lychee_ripe_detection/weights/
├── best.pt    # 验证集上表现最好的模型（用于推理）
└── last.pt    # 最后一轮的模型
```

### 10.6 常见问题

#### Q1: 应该用成熟度检测还是物候期识别？

**答案**：取决于你的应用场景

- **如果想判断何时采摘荔枝**：使用成熟度检测（LycheeRipeDetection）
- **如果想判断植株生长阶段，指导农事管理**：使用物候期识别（需要新建数据集）

#### Q2: 可以将物候期数据混入成熟度数据集吗？

**答案**：❌ **不可以**

**原因**：
1. **标注对象不同**：成熟度标注果实，物候期标注植株
2. **类别不同**：成熟度是4个成熟度阶段，物候期是多个生长阶段
3. **应用场景不同**：成熟度用于采摘，物候期用于农事管理

**建议**：分别为两个任务创建独立的数据集

#### Q3: 如何判断数据标注是否正确？

**检查方法**：
1. **查看标签文件**：检查类别ID是否正确（0=秋梢期，1=白点期）
2. **可视化标注**：使用标注工具打开图像，查看边界框是否正确
3. **统计类别分布**：检查各类别的样本数量是否平衡

**示例检查脚本**：
```python
import os
from collections import Counter

# 统计训练集各类别的数量
train_labels = "labels/train/"
class_counts = Counter()

for label_file in os.listdir(train_labels):
    if label_file.endswith('.txt'):
        with open(os.path.join(train_labels, label_file), 'r') as f:
            for line in f:
                if line.strip():
                    class_id = int(line.split()[0])
                    class_counts[class_id] += 1

print("训练集类别分布：")
for class_id, count in sorted(class_counts.items()):
    class_names = {0: "秋梢期", 1: "白点期"}
    print(f"  {class_names[class_id]}: {count} 个")
```

#### Q4: 训练需要多少数据？

**建议**：
- **每个类别至少100-200张图像**（训练集）
- **验证集至少20-50张图像**（每个类别）
- **数据越多越好**，但要注意数据质量

**你的场景**：
- 秋梢期和白点期：每个类别建议至少准备100张标注好的图像
- 如果数据不足，可以通过数据增强来扩充数据集

### 10.7 最佳实践建议

1. **数据准备**：
   - ✅ 使用高质量、清晰的图像
   - ✅ 确保标注准确无误
   - ✅ 保持各类别样本数量平衡
   - ✅ 图像要多样化（不同角度、光照条件等）

2. **训练设置**：
   - ✅ 先用小数据集和少量epoch测试流程（如10个epoch）
   - ✅ 确认流程正确后，再完整训练
   - ✅ 监控训练过程，查看loss曲线和mAP指标

3. **模型选择**：
   - ✅ 数据量少（<1000张）：使用 `yolov8n.pt`（更快）
   - ✅ 数据量多（>1000张）：使用 `yolov8s.pt` 或 `yolov8m.pt`（更准确）

4. **部署建议**：
   - ✅ 导出ONNX格式（更快、更易部署）
   - ✅ 使用GPU加速推理（比CPU快很多）
   - ✅ 调整置信度阈值（根据实际应用场景）

---

## 十一、小目标检测优化方案：秋梢期与白点期检测

### 11.1 问题背景

#### 11.1.1 实际场景

**新数据特点**：
- **时间**：2025年11月至2026年1月（大寒节气期间）
- **数据来源**：低空监控摄像头拍摄
- **图像尺寸**：1920×1080（高分辨率）
- **检测对象**：秋梢期和露白点期的荔枝植株/枝条
- **目标特点**：**目标非常小**，在1920×1080图像中可能只占几十到几百个像素

**核心问题**：
1. **目标尺寸小**：秋梢和白点在1920×1080图像中可能只占 20×20 到 100×100 像素
2. **分辨率压缩**：如果直接resize到640×640，小目标会变得更小（约缩小到原来的1/3）
3. **识别困难**：传统640×640训练可能无法有效识别如此小的目标

#### 11.1.2 为什么需要特殊优化？

**成熟度检测（v5.onnx）vs 物候期检测（best.pt）对比**：

| 特征 | 成熟度检测 | 物候期检测（新需求） |
|------|----------|-------------------|
| **检测对象** | 荔枝果实（较大） | 植株/枝条（较小） |
| **目标大小** | 50×50 到 200×200 像素 | 20×20 到 100×100 像素 |
| **图像尺寸** | 1920×1080 | 1920×1080 |
| **压缩后大小** | 17×17 到 67×67 像素 | **7×7 到 33×33 像素** ⚠️ |
| **识别难度** | 容易（目标大） | **困难（目标小）** ⚠️ |

**关键问题**：
- 如果目标在1920×1080中只有30×30像素
- Resize到640×360后只有 **10×10像素**
- 这对于YOLO模型来说太小了，识别准确率会大幅下降

#### 11.1.3 图像尺寸处理原理

**Letterbox Resize详解**：

```python
# Letterbox Resize 的过程（从代码可见）
# 1920×1080 的图像 → 640×640

步骤1：计算缩放比例（保持宽高比）
  原始图像：1920×1080（宽高比 16:9）
  目标尺寸：640×640（宽高比 1:1）
  缩放比例 r = min(640/1080, 640/1920) = min(0.593, 0.333) = 0.333

步骤2：等比例缩放（不压缩，只缩小）
  新尺寸 = 1920×0.333 × 1080×0.333 = 640×360
  不是压缩整个图像，而是等比例缩放，保持宽高比

步骤3：添加灰色填充（Letterbox）
  上下各填充 (640-360)/2 = 140像素的灰色
  最终尺寸 = 640×640（有效图像区域是 640×360）
```

**实际影响**：

| 原图尺寸 | 目标在原图中的大小 | Resize后的大小 | 压缩比例 |
|---------|-----------------|---------------|---------|
| **1920×1080** | 100×100 像素 | 33×33 像素 | **33.3%** |
| **1920×1080** | 50×50 像素 | **17×17 像素** | **33.3%** ⚠️ |
| **1920×1080** | 30×30 像素 | **10×10 像素** | **33.3%** ⚠️ |

**为什么640×640对于小目标不够？**

- **YOLO模型的设计**：YOLOv8在640×640上训练，主要针对中等大小的目标（> 32×32像素）
- **小目标问题**：小目标（< 32×32像素）在特征图上可能只有1-2个像素
- **模型限制**：模型难以学习到小目标的特征

### 11.2 完整优化方案

#### 11.2.1 方案一：使用更大的输入尺寸（推荐）

**原理**：使用更大的输入尺寸，保留更多细节

**训练参数配置**：

```python
from ultralytics import YOLO

# 加载预训练模型
model = YOLO("yolov8n.pt")  # 或者 yolov8s.pt（更大，更准确）

if __name__ == '__main__':
    # 使用1280×1280输入尺寸训练（保留更多细节）
    model.train(
        data="new_dataset/data.yaml",  # 秋梢期和白点期数据集配置
        epochs=300,                    # 训练轮数
        imgsz=1280,                    # ⭐ 使用1280×1280（而不是640×640）
        batch=8,                       # ⭐ batch size减小（因为图像更大，内存占用更多）
        patience=100,                  # 早停耐心值
        name="lychee_phenological_1280",  # 训练结果文件夹名称
        
        # 针对小目标的优化参数
        conf=0.3,                      # ⭐ 降低置信度阈值（小目标需要更宽松）
        iou=0.5,                       # IoU阈值
        max_det=1000,                  # ⭐ 允许检测更多目标
        augment=True,                  # ⭐ 开启数据增强（对小目标很重要）
        
        # 数据增强参数（针对小目标优化）
        mosaic=1.0,                    # Mosaic增强（将4张图像拼接）
        mixup=0.1,                     # Mixup增强（轻微混合）
        copy_paste=0.1,                # ⭐ Copy-paste增强（复制小目标到其他图像）
        degrees=10.0,                  # 旋转增强（±10度）
        translate=0.1,                 # 平移增强
        scale=0.5,                     # 缩放增强
        shear=5.0,                     # 剪切增强
        perspective=0.0,               # 透视增强
        flipud=0.0,                    # 上下翻转
        fliplr=0.5,                    # 左右翻转（50%概率）
        hsv_h=0.015,                   # 色调增强
        hsv_s=0.7,                     # 饱和度增强
        hsv_v=0.4,                     # 亮度增强
    )
    
    # 在验证集上评估模型性能
    metrics = model.val()
    print(f"mAP50: {metrics.box.map50}")
    print(f"训练完成！模型保存在 runs/detect/lychee_phenological_1280/weights/best.pt")
```

**推理代码**：

```python
from ultralytics import YOLO

# 加载训练好的模型
model = YOLO("runs/detect/lychee_phenological_1280/weights/best.pt")

# 对1920×1080图像进行检测（自动resize到1280×1280）
results = model.predict(
    source="your_1920x1080_image.jpg",
    imgsz=1280,              # ⭐ 使用1280×1280输入尺寸
    conf=0.3,                # ⭐ 降低置信度阈值（小目标需要更宽松）
    iou=0.5,
    save=True,
    show=False
)

# 查看结果
for result in results:
    boxes = result.boxes
    print(f"检测到 {len(boxes)} 个目标")
    
    for box in boxes:
        class_id = int(box.cls[0])
        confidence = float(box.conf[0])
        class_name = result.names[class_id]
        x1, y1, x2, y2 = box.xyxy[0].tolist()
        
        # 计算目标大小
        target_width = x2 - x1
        target_height = y2 - y1
        
        print(f"\n检测到: {class_name}")
        print(f"  置信度: {confidence:.2%}")
        print(f"  位置: ({x1:.0f}, {y1:.0f}, {x2:.0f}, {y2:.0f})")
        print(f"  大小: {target_width:.0f}×{target_height:.0f} 像素")
```

**优势**：
- ✅ 保留更多细节（1920×1080 → 1280×720，压缩比例从33.3%提升到66.7%）
- ✅ 小目标更大（30×30 → 20×20像素，更容易识别）
- ✅ 训练和推理统一，效果好

**劣势**：
- ❌ 训练时间增加（约4倍）
- ❌ 内存占用增加（约4倍）
- ❌ 推理速度变慢（约4倍）

**适用场景**：如果GPU内存足够（> 16GB），推荐使用

#### 11.2.2 方案二：图像切片 + 合并结果（最准确）

**原理**：将大图像切分成多个小块，每个小块都是640×640，无压缩损失

**图像切片代码**：

```python
import cv2
import numpy as np
from ultralytics import YOLO
from pathlib import Path

def split_image(image_path, tile_size=640, overlap=160):
    """
    将大图像切片成多个小块，重叠部分可以避免遗漏目标
    
    参数:
        image_path: 图像路径
        tile_size: 切片大小（默认640×640）
        overlap: 重叠像素数（默认160，即25%重叠）
    
    返回:
        tiles: 切片图像列表
        positions: 切片在原图中的位置列表
    """
    img = cv2.imread(image_path)
    h, w = img.shape[:2]
    
    tiles = []
    positions = []
    
    # 计算步长（考虑重叠）
    stride = tile_size - overlap
    
    for y in range(0, h, stride):
        for x in range(0, w, stride):
            # 提取切片
            y_end = min(y + tile_size, h)
            x_end = min(x + tile_size, w)
            tile = img[y:y_end, x:x_end]
            
            # 如果切片不够大，填充灰色
            if tile.shape[0] < tile_size or tile.shape[1] < tile_size:
                top = (tile_size - tile.shape[0]) // 2
                bottom = tile_size - tile.shape[0] - top
                left = (tile_size - tile.shape[1]) // 2
                right = tile_size - tile.shape[1] - left
                
                tile = cv2.copyMakeBorder(
                    tile, top, bottom, left, right,
                    cv2.BORDER_CONSTANT, value=(114, 114, 114)
                )
            
            tiles.append(tile)
            positions.append((x, y))
    
    return tiles, positions

def merge_results(results_list, positions, original_shape):
    """
    合并多个切片的检测结果，去除重复检测
    
    参数:
        results_list: 每个切片的检测结果列表
        positions: 切片在原图中的位置列表
        original_shape: 原图像尺寸 (height, width)
    
    返回:
        merged_results: 合并后的检测结果
    """
    all_boxes = []
    all_confidences = []
    all_class_ids = []
    
    # 收集所有检测结果，并转换坐标
    for results, (x_offset, y_offset) in zip(results_list, positions):
        boxes = results.boxes
        for box in boxes:
            x1, y1, x2, y2 = box.xyxy[0].tolist()
            
            # 转换回原图坐标
            x1 += x_offset
            y1 += y_offset
            x2 += x_offset
            y2 += y_offset
            
            all_boxes.append([x1, y1, x2, y2])
            all_confidences.append(float(box.conf[0]))
            all_class_ids.append(int(box.cls[0]))
    
    # 使用NMS去除重复检测
    if len(all_boxes) > 0:
        import torch
        boxes_tensor = torch.tensor(all_boxes)
        scores_tensor = torch.tensor(all_confidences)
        
        # 应用NMS（IoU阈值0.5）
        from torchvision.ops import nms
        keep_indices = nms(boxes_tensor, scores_tensor, iou_threshold=0.5)
        
        # 保留NMS后的检测结果
        merged_boxes = [all_boxes[i] for i in keep_indices]
        merged_confidences = [all_confidences[i] for i in keep_indices]
        merged_class_ids = [all_class_ids[i] for i in keep_indices]
        
        return {
            'boxes': merged_boxes,
            'confidences': merged_confidences,
            'class_ids': merged_class_ids
        }
    
    return {'boxes': [], 'confidences': [], 'class_ids': []}

# 使用示例
def detect_with_tiling(model, image_path, tile_size=640, overlap=160, conf=0.3):
    """
    使用图像切片进行检测
    """
    # 1. 切片图像
    tiles, positions = split_image(image_path, tile_size, overlap)
    print(f"图像切片完成：共 {len(tiles)} 个切片")
    
    # 2. 对每个切片进行检测
    results_list = []
    for i, tile in enumerate(tiles):
        print(f"检测切片 {i+1}/{len(tiles)}...")
        
        # 保存临时切片（用于检测）
        temp_path = f"temp_tile_{i}.jpg"
        cv2.imwrite(temp_path, tile)
        
        # 检测切片
        result = model.predict(
            source=temp_path,
            imgsz=tile_size,
            conf=conf,
            save=False
        )
        results_list.append(result[0])
        
        # 删除临时文件
        Path(temp_path).unlink()
    
    # 3. 合并结果
    original_img = cv2.imread(image_path)
    original_shape = original_img.shape[:2]
    merged_results = merge_results(results_list, positions, original_shape)
    
    print(f"\n合并后检测到 {len(merged_results['boxes'])} 个目标")
    
    return merged_results

# 主程序
if __name__ == '__main__':
    # 加载模型
    model = YOLO("runs/detect/train6/weights/best.pt")
    
    # 检测图像（使用切片）
    results = detect_with_tiling(
        model,
        "your_1920x1080_image.jpg",
        tile_size=640,
        overlap=160,  # 25%重叠，避免遗漏边缘目标
        conf=0.3
    )
    
    # 可视化结果（在原图上绘制）
    img = cv2.imread("your_1920x1080_image.jpg")
    for box, conf, class_id in zip(
        results['boxes'],
        results['confidences'],
        results['class_ids']
    ):
        x1, y1, x2, y2 = map(int, box)
        cv2.rectangle(img, (x1, y1), (x2, y2), (0, 255, 0), 2)
        label = f"Class {class_id}: {conf:.2f}"
        cv2.putText(img, label, (x1, y1-5), cv2.FONT_HERSHEY_SIMPLEX, 0.5, (0, 255, 0), 2)
    
    cv2.imwrite("result.jpg", img)
```

**优势**：
- ✅ **无压缩损失**（每个切片都是640×640，完整保留细节）
- ✅ **适合小目标**（目标不会被压缩）
- ✅ **最准确**（理论上最准确的方法）

**劣势**：
- ❌ **推理时间较长**（需要处理多个切片）
- ❌ **需要合并结果**（需要NMS去重）
- ❌ **代码复杂**（需要实现切片和合并逻辑）

**适用场景**：如果追求最高准确率，且可以接受较长的推理时间

#### 11.2.3 方案三：多尺度训练（最平衡）

**原理**：训练时使用多种输入尺寸，模型适应不同尺寸的目标

**训练参数配置**：

```python
from ultralytics import YOLO

# 加载预训练模型
model = YOLO("yolov8n.pt")

if __name__ == '__main__':
    # 多尺度训练：训练时随机使用不同尺寸
    model.train(
        data="new_dataset/data.yaml",
        epochs=300,
        imgsz=[640, 800, 1024, 1280],  # ⭐ 多尺度训练（随机选择）
        batch=8,                        # batch size需要根据最小尺寸调整
        patience=100,
        name="lychee_phenological_multi_scale",
        
        # 针对小目标的优化
        conf=0.3,
        iou=0.5,
        augment=True,
        
        # 数据增强
        mosaic=1.0,
        mixup=0.1,
        copy_paste=0.1,  # ⭐ 对小目标很有效
        degrees=10.0,
        translate=0.1,
        scale=0.5,
    )
```

**推理代码**：

```python
from ultralytics import YOLO

model = YOLO("runs/detect/lychee_phenological_multi_scale/weights/best.pt")

# 推理时可以选择合适的尺寸
results = model.predict(
    source="your_1920x1080_image.jpg",
    imgsz=1280,  # 可以选择640、800、1024或1280
    conf=0.3,
    save=True
)
```

**优势**：
- ✅ **适应性强**（模型学习到不同尺寸的特征）
- ✅ **推理灵活**（可以根据实际情况选择输入尺寸）
- ✅ **效果稳定**（多尺度训练通常效果更好）

**劣势**：
- ❌ **训练时间更长**（需要处理多种尺寸）
- ❌ **内存占用增加**（需要处理最大尺寸）

**适用场景**：如果想在准确率和速度之间取得平衡

### 11.3 数据集准备建议

#### 11.3.1 数据标注注意事项

**对于小目标的标注**：

1. **标注要精确**：
   - ✅ 边界框要紧凑（不要包含太多背景）
   - ✅ 边界框要完整（不要遗漏目标）
   - ❌ 不要标注得太宽松（会增加背景噪声）

2. **标注示例**：
   ```
   好的标注：
   - 秋梢期：框住新梢的尖端部分（包括1-2片新叶）
   - 白点期：框住花芽的白色小点（尽量紧凑）
   
   不好的标注：
   - 秋梢期：框住整棵大树（包含太多背景）
   - 白点期：框住整个花芽（不够精确）
   ```

3. **数据质量检查脚本**：
```python
# check_annotation_sizes.py
import os
import cv2
from collections import defaultdict

def check_annotation_sizes(image_dir, label_dir):
    """检查标注的目标大小分布"""
    size_distribution = defaultdict(int)
    
    for label_file in os.listdir(label_dir):
        if not label_file.endswith('.txt'):
            continue
        
        # 读取标签文件
        image_name = label_file.replace('.txt', '.jpg')
        image_path = os.path.join(image_dir, image_name)
        
        if not os.path.exists(image_path):
            continue
        
        img = cv2.imread(image_path)
        h, w = img.shape[:2]
        
        # 读取标注
        with open(os.path.join(label_dir, label_file), 'r') as f:
            for line in f:
                parts = line.strip().split()
                if len(parts) >= 5:
                    class_id = int(parts[0])
                    x_center = float(parts[1]) * w
                    y_center = float(parts[2]) * w
                    width = float(parts[3]) * w
                    height = float(parts[4]) * h
                    
                    # 分类目标大小
                    area = width * height
                    if area < 100:  # < 10×10
                        size_distribution['tiny'] += 1
                    elif area < 400:  # < 20×20
                        size_distribution['small'] += 1
                    elif area < 1600:  # < 40×40
                        size_distribution['medium'] += 1
                    else:
                        size_distribution['large'] += 1
    
    print("目标大小分布：")
    print(f"  极小目标（< 10×10）: {size_distribution['tiny']} 个")
    print(f"  小目标（10×10 - 20×20）: {size_distribution['small']} 个")
    print(f"  中等目标（20×20 - 40×40）: {size_distribution['medium']} 个")
    print(f"  大目标（> 40×40）: {size_distribution['large']} 个")
    
    # 如果小目标太多，建议使用更大的输入尺寸
    total = sum(size_distribution.values())
    if total > 0:
        small_ratio = (size_distribution['tiny'] + size_distribution['small']) / total
        if small_ratio > 0.5:
            print("\n⚠️ 警告：小目标占比超过50%，建议使用1280×1280或图像切片方案")

# 使用示例
if __name__ == '__main__':
    check_annotation_sizes(
        "new_dataset/images/train",
        "new_dataset/labels/train"
    )
```

#### 11.3.2 数据增强建议

**针对小目标的增强策略**：

1. **Mosaic增强**（强烈推荐）：
   - 将4张图像拼接成一张
   - 增加小目标的数量和多样性
   - 提高模型的泛化能力

2. **Copy-Paste增强**（推荐）：
   - 复制小目标到其他图像
   - 增加小目标的训练样本
   - 对数据量少的小目标特别有效

3. **旋转和缩放**：
   - 轻微旋转（±10度）
   - 轻微缩放（0.5-1.5倍）
   - 增加数据的多样性

4. **光照增强**：
   - 调整色调、饱和度、亮度
   - 模拟不同光照条件
   - 提高模型的鲁棒性

### 11.4 训练参数优化总结

#### 11.4.1 关键参数对比

| 参数 | 标准训练（640×640） | 小目标优化（1280×1280） | 说明 |
|------|------------------|----------------------|------|
| **imgsz** | 640 | **1280** | 更大尺寸保留更多细节 |
| **batch** | 16 | **8** | 图像更大，需要减小batch |
| **conf** | 0.5 | **0.3** | 小目标需要更宽松的阈值 |
| **max_det** | 300 | **1000** | 允许检测更多目标 |
| **mosaic** | 1.0 | **1.0** | 保持Mosaic增强 |
| **copy_paste** | 0.0 | **0.3** | ⭐ 对小目标很重要 |
| **degrees** | 0.0 | **10.0** | 轻微旋转增强 |
| **scale** | 0.5 | **0.5** | 保持缩放增强 |

#### 11.4.2 推荐配置

**配置1：快速测试（640×640）**

```python
# test_640.py
from ultralytics import YOLO

model = YOLO("yolov8n.pt")
model.train(
    data="new_dataset/data.yaml",
    epochs=50,           # 先少量epoch测试
    imgsz=640,
    batch=16,
    conf=0.3,             # 降低置信度
    name="test_640"
)
```

**配置2：平衡方案（1280×1280）⭐推荐**

```python
# train_1280.py
from ultralytics import YOLO

model = YOLO("yolov8n.pt")
model.train(
    data="new_dataset/data.yaml",
    epochs=300,
    imgsz=1280,           # ⭐ 使用1280×1280
    batch=8,              # ⭐ 减小batch
    conf=0.3,
    copy_paste=0.3,       # ⭐ Copy-Paste增强
    name="lychee_1280"
)
```

**配置3：最佳方案（多尺度）**

```python
# train_multi_scale.py
from ultralytics import YOLO

model = YOLO("yolov8n.pt")
model.train(
    data="new_dataset/data.yaml",
    epochs=300,
    imgsz=[640, 800, 1024, 1280],  # ⭐ 多尺度训练
    batch=8,
    conf=0.3,
    copy_paste=0.3,
    name="lychee_multi_scale"
)
```

### 11.5 实际实施步骤

#### 步骤1：准备数据集

```bash
# 1. 创建数据集目录
mkdir -p new_dataset/{images,labels}/{train,val}

# 2. 组织数据
# - 将1920×1080的图像放入 images/train/ 和 images/val/
# - 使用LabelMe标注秋梢期和白点期
# - 转换为YOLO格式，放入 labels/train/ 和 labels/val/
```

#### 步骤2：检查数据质量

```python
# 运行数据质量检查脚本
python check_annotation_sizes.py
# 查看目标大小分布，决定使用哪种方案
```

#### 步骤3：创建data.yaml

```yaml
# new_dataset/data.yaml
train: images/train
val: images/val

names:
  0: "QiuShao"     # 秋梢期
  1: "LuBaiDian"   # 露白点期
```

#### 步骤4：选择训练方案

**方案A：先测试640×640**

```python
# test_640.py
from ultralytics import YOLO

model = YOLO("yolov8n.pt")
model.train(
    data="new_dataset/data.yaml",
    epochs=50,        # 先少量epoch测试
    imgsz=640,
    batch=16,
    conf=0.3,
    name="test_640"
)
```

**方案B：使用1280×1280（推荐）**

```python
# train_1280.py
from ultralytics import YOLO

model = YOLO("yolov8n.pt")
model.train(
    data="new_dataset/data.yaml",
    epochs=300,
    imgsz=1280,
    batch=8,
    conf=0.3,
    copy_paste=0.3,
    name="lychee_1280"
)
```

#### 步骤5：评估效果

```python
# evaluate.py
from ultralytics import YOLO

model = YOLO("runs/detect/lychee_1280/weights/best.pt")

# 在验证集上评估
metrics = model.val()
print(f"mAP50: {metrics.box.map50}")
print(f"mAP50-95: {metrics.box.map}")

# 如果mAP50 < 0.5，考虑：
# 1. 使用图像切片方案
# 2. 增加训练数据
# 3. 使用更大的模型（yolov8s, yolov8m）
```

### 11.6 常见问题解答

#### Q1: 我应该使用哪种方案？

**答案**：根据实际情况选择

1. **如果GPU内存足够（> 16GB）**：使用方案一（1280×1280）⭐推荐
2. **如果追求最高准确率**：使用方案二（图像切片）
3. **如果想平衡准确率和速度**：使用方案三（多尺度训练）
4. **如果不确定**：先用640×640测试，根据效果决定

#### Q2: 训练需要多长时间？

**答案**：取决于配置

| 配置 | 训练时间（估算） | GPU内存需求 |
|------|----------------|------------|
| 640×640, batch=16 | 2-4小时 | 8GB |
| 1280×1280, batch=8 | 8-12小时 | 16GB |
| 多尺度训练 | 12-16小时 | 16GB |

#### Q3: 如果效果还是不好怎么办？

**答案**：考虑以下优化

1. **增加训练数据**：收集更多标注数据
2. **使用更大的模型**：从yolov8n升级到yolov8s或yolov8m
3. **迁移学习**：使用已有的best.pt作为预训练模型
4. **调整数据增强**：增加copy_paste比例
5. **调整学习率**：降低学习率，训练更充分

### 11.7 总结

#### 核心要点

1. **小目标检测需要特殊优化**：
   - 1920×1080图像中的小目标（< 50×50像素）
   - Resize到640×640后会变得更小（< 17×17像素）
   - 传统640×640训练可能无法有效识别

2. **推荐方案**：
   - **首选**：使用1280×1280训练（平衡准确率和速度）⭐
   - **最佳**：图像切片（最高准确率，但更复杂）
   - **平衡**：多尺度训练（适应性强）

3. **关键参数**：
   - `imgsz=1280`：使用更大的输入尺寸
   - `conf=0.3`：降低置信度阈值
   - `copy_paste=0.3`：Copy-Paste增强对小目标很重要

4. **实施建议**：
   - 先用640×640测试效果
   - 如果效果不好，升级到1280×1280
   - 如果仍然不够，考虑图像切片或多尺度训练

---

## 附录：快速参考

### A. 训练命令
```bash
python runPeriod_detection.py
```

### B. 推理命令（PyTorch）
```python
model = YOLO("best.pt")
results = model.predict("image.jpg")
```

### C. 推理命令（ONNX）
```python
detector = Detector("model.onnx")
results = detector.detect("image.jpg")
```

### D. API服务
```bash
python main.py
```

---

**文档版本**：v1.0  
**最后更新**：2026-01-19  
**适用项目**：荔枝成熟度检测（Immature、Turing、Coloring、Ripe）

如有问题，请参考代码注释或联系项目维护者。

---

## 附录：文件夹结构说明

### 1. 13_公开数据集 (`e:\基于无人机与AI的荔枝产业应用\13_公开数据集`)

该文件夹包含了用于训练和测试的各类荔枝相关数据集，是项目的核心数据资源库。

#### 1.1 完整目录结构

```
13_公开数据集/
├── Datasets/                          # 主要数据集目录
│   ├── Classification/                # 分类任务数据集
│   │   ├── CitrusDisease/            # 柑橘病害分类（10类病害）
│   │   │   ├── train/                # 训练集（599张图像）
│   │   │   │   ├── FruitBlackSpot/   # 果实黑斑病
│   │   │   │   ├── FruitCanker/      # 果实溃疡病
│   │   │   │   ├── FruitGreening/    # 果实黄龙病
│   │   │   │   ├── HealthyFruit/     # 健康果实
│   │   │   │   ├── HealthyLeave/     # 健康叶片
│   │   │   │   ├── LeaveBlackSpot/   # 叶片黑斑病
│   │   │   │   ├── LeaveCanker/      # 叶片溃疡病
│   │   │   │   ├── LeaveGreening/    # 叶片黄龙病
│   │   │   │   ├── Melanose/         # 褐斑病
│   │   │   │   └── Scab/             # 疮痂病
│   │   │   ├── test/                 # 测试集（154张图像）
│   │   │   └── *.cache               # 缓存文件
│   │   │
│   │   ├── Flower/                   # 花卉分类数据集
│   │   │   ├── train/                # 训练集（6497张图像）
│   │   │   └── test/                 # 测试集（1676张图像）
│   │   │
│   │   ├── ForestFire/               # 森林火灾检测（二分类）
│   │   │   ├── train/
│   │   │   │   ├── Fire/             # 火灾图像（1911张）
│   │   │   │   └── NonFire/          # 非火灾图像（1548张）
│   │   │   └── test/
│   │   │       ├── Fire/             # 火灾图像（478张）
│   │   │       └── NonFire/          # 非火灾图像（387张）
│   │   │
│   │   ├── Fruit/                    # 水果分类数据集（19种水果）
│   │   │   ├── train/                # 训练集（1429张图像）
│   │   │   └── test/                 # 测试集（470张图像）
│   │   │
│   │   ├── FruitRawRipe/             # 水果生熟度分类
│   │   │   ├── train/                # 训练集（1216张图像）
│   │   │   └── test/                 # 测试集（415张图像）
│   │   │
│   │   ├── LycheeInsectPests/        # 荔枝虫害分类（7类虫害）
│   │   │   ├── train/                # 训练集（517张图像）
│   │   │   │   ├── DaXiShuai/        # 大蟋蟀
│   │   │   │   ├── FeiZhouLouGu/     # 非洲蝼蛄
│   │   │   │   ├── LiZhiChun/        # 荔枝蝽
│   │   │   │   ├── LongYanJi/        # 龙眼鸡
│   │   │   │   ├── MaPiChun/         # 麻皮蝽
│   │   │   │   ├── PoChun/           # 珀蝽
│   │   │   │   └── XingTianNiu/      # 星天牛
│   │   │   └── test/                 # 测试集（132张图像）
│   │   │
│   │   ├── LycheePhenologicalStages/ # 荔枝物候期分类（5个阶段）
│   │   │   ├── train/                # 训练集（783张图像）
│   │   │   │   ├── HuaSui/           # 华穗期
│   │   │   │   ├── JieGuo/           # 结果期
│   │   │   │   ├── KaiHua/           # 开花期
│   │   │   │   ├── LuBaiDian/        # 露白点期
│   │   │   │   └── QiuShao/          # 秋梢期
│   │   │   └── test/                 # 测试集（198张图像）
│   │   │
│   │   ├── TeaDisease/               # 茶叶病害分类
│   │   │   ├── train/                # 训练集（5127张图像）
│   │   │   └── test/                 # 测试集（1288张图像）
│   │   │
│   │   ├── TobaccoDisease/           # 烟草病害分类
│   │   │   ├── train/                # 训练集（240张图像）
│   │   │   └── test/                 # 测试集（61张图像）
│   │   │
│   │   ├── Plain/                    # 原始未分类数据（用于数据集划分）
│   │   │   ├── CitrusDiseasePlain/   # 柑橘病害原始图像（753张）
│   │   │   ├── FlowerPlain/          # 花卉原始图像（8173张）
│   │   │   ├── ForestFirePlain/      # 森林火灾原始图像（4324张）
│   │   │   ├── FruitPlain/           # 水果原始图像（1627张）
│   │   │   ├── TeaDiseasePlain/      # 茶叶病害原始图像（6415张）
│   │   │   ├── TeaInsectPestsPlain/  # 茶叶虫害原始图像（69张）
│   │   │   └── TobaccoDiseasePlain/  # 烟草病害原始图像（301张）
│   │   │
│   │   └── split_plain.py            # 数据集划分脚本（80:20划分）
│   │
│   ├── Detection/                    # 目标检测数据集（YOLO格式）
│   │   ├── CanopyDetection/          # 树冠检测数据集
│   │   │   ├── data.yaml             # 数据集配置文件
│   │   │   ├── README.dataset.txt    # 数据集说明
│   │   │   ├── README.roboflow.txt   # Roboflow平台说明
│   │   │   ├── seg_2_box.py          # 分割标签转检测标签脚本
│   │   │   ├── train/                # 训练集（272张图像+272个标签）
│   │   │   │   ├── images/
│   │   │   │   └── labels/
│   │   │   └── valid/                # 验证集（116张图像+116个标签）
│   │   │       ├── images/
│   │   │       └── labels/
│   │   │
│   │   ├── Citrus-CidDet-YOLO/       # 柑橘病害检测数据集
│   │   │   ├── data.yaml             # 数据集配置文件
│   │   │   ├── train/                # 训练集（391张图像+391个标签）
│   │   │   ├── val/                  # 验证集（69张图像+69个标签）
│   │   │   └── test/                 # 测试集（119张图像+119个标签）
│   │   │
│   │   ├── LycheeRipeDetection/      # 荔枝成熟度检测（本指南核心数据集）
│   │   │   ├── data.yaml             # 数据集配置文件（4类：Immature, Turing, Coloring, Ripe）
│   │   │   ├── images/               # 图像目录（396张）
│   │   │   │   ├── train/            # 训练集图像（295张）
│   │   │   │   └── val/              # 验证集图像（101张）
│   │   │   └── labels/               # YOLO格式标签目录（396个txt文件）
│   │   │       ├── train/            # 训练集标签（295个）
│   │   │       └── val/              # 验证集标签（101个）
│   │   │
│   │   └── Plain/                    # 原始检测数据
│   │       ├── LycheeCamera/         # 荔枝相机采集的原始图像
│   │       │   ├── FT2526603/        # 相机1：按日期组织的原始图像（约2800张）
│   │       │   │   ├── 20250509/     # 2025年5月9日采集的图像
│   │       │   │   ├── 20250510/     # 2025年5月10日采集的图像
│   │       │   │   ├── ...           # 其他日期（5月-7月）
│   │       │   │   └── 20250703/     # 最后一个日期
│   │       │   ├── FY4858258/        # 相机2：按日期组织的原始图像（约2800张）
│   │       │   │   ├── 20250511/     # 2025年5月11日采集的图像
│   │       │   │   ├── ...           # 其他日期（5月-7月）
│   │       │   │   └── 20250703/     # 最后一个日期
│   │       │   ├── collected/        # 按成熟度分类收集的图像
│   │       │   │   ├── Immature/     # 未成熟期（36张）
│   │       │   │   ├── Turning/      # 转色期（170张）
│   │       │   │   ├── Coloring/     # 着色期（95张）
│   │       │   │   └── Ripe/         # 成熟期（85张）
│   │       │   └── main.py           # 数据收集脚本（按日期映射收集成熟期图像）
│   │       │
│   │       ├── LycheeRipe/           # 荔枝成熟度原始数据
│   │       │   ├── images/           # 原始图像（322张）
│   │       │   ├── labels/           # 原始标签（322个txt文件）
│   │       │   └── classes.txt       # 类别名称文件（4类）
│   │       │
│   │       └── split.py              # 数据集划分脚本（80:20划分）
│   │
│   └── Segmentation/                 # 语义分割数据集
│       └── CanopySegmentation/       # 树冠分割数据集
│           ├── data.yaml             # 数据集配置文件
│           ├── README.dataset.txt    # 数据集说明
│           ├── README.roboflow.txt   # Roboflow平台说明
│           ├── train/                # 训练集（272张图像+272个分割标签）
│           └── valid/                # 验证集（116张图像+116个分割标签）
│
└── 编码/                             # 编码对照表目录
    ├── fruit.csv                     # 水果编码表（19种水果的中英文对照）
    ├── orange.csv                    # 柑橘编码表
    ├── teatree.csv                   # 茶树编码表
    ├── total.csv                     # 汇总编码表（所有类别的编码对照）
    ├── total.xlsx                    # 汇总编码表（Excel格式）
    ├── 水果对应的编码.xlsx            # 水果编码表（Excel格式，19种水果）
    ├── 病害对应编码.xlsx              # 病害编码表（Excel格式）
    ├── 花卉对应编码.xlsx              # 花卉编码表（Excel格式）
    └── 虫害对应编码.xlsx              # 虫害编码表（Excel格式，包含7种荔枝虫害）
```

#### 1.2 关键数据集说明

##### 1.2.1 荔枝成熟度检测数据集（核心）

**位置**：`Datasets/Detection/LycheeRipeDetection/`

**用途**：训练和验证荔枝成熟度检测模型（4类：Immature, Turing, Coloring, Ripe）

**数据量**：
- 训练集：295张图像 + 295个YOLO格式标签文件
- 验证集：101张图像 + 101个YOLO格式标签文件
- 总计：396张图像 + 396个标签文件

**数据结构**：
```
LycheeRipeDetection/
├── data.yaml                    # 配置文件，定义类别：["Immature", "Turing", "Coloring", "Ripe"]
├── images/
│   ├── train/                  # 训练集图像
│   └── val/                    # 验证集图像
└── labels/
    ├── train/                  # 训练集标签（YOLO格式：class_id x_center y_center width height）
    └── val/                    # 验证集标签
```

**标签格式示例**：
```
3 0.306640 0.573375 0.023168 0.057632
3 0.371841 0.564145 0.022954 0.040210
```
- `3`：类别ID（Ripe成熟荔枝）
- `0.306640 0.573375`：边界框中心点坐标（归一化）
- `0.023168 0.057632`：边界框宽度和高度（归一化）

##### 1.2.2 荔枝相机原始数据

**位置**：`Datasets/Detection/Plain/LycheeCamera/`

**用途**：存储相机采集的原始荔枝图像，按日期组织

**数据来源**：
- **相机1（FT2526603）**：从2025年5月9日到7月3日，每天采集约39-78张图像
- **相机2（FY4858258）**：从2025年5月11日到7月3日，每天采集约39张图像
- **总计**：约5600张原始图像

**数据组织**：
- 按日期文件夹组织（如`20250509/`、`20250619/`等）
- 文件名格式：`相机ID_日期时间.jpg`（如`FT2526603_20250619181901.jpg`）
- 已分类收集的图像存储在`collected/`目录下，按成熟度阶段分类

**数据收集脚本**：`main.py`
- 根据日期映射表自动识别成熟度阶段
- 从每个日期文件夹中采样图像（最多4张）
- 复制到对应的成熟度类别文件夹

##### 1.2.3 荔枝物候期分类数据集

**位置**：`Datasets/Classification/LycheePhenologicalStages/`

**用途**：训练荔枝物候期分类模型（5个阶段）

**数据量**：
- 训练集：783张图像
- 测试集：198张图像

**类别**：
1. **HuaSui（华穗期）**：花穗抽出期
2. **JieGuo（结果期）**：果实形成期
3. **KaiHua（开花期）**：花朵开放期
4. **LuBaiDian（露白点期）**：花芽分化期
5. **QiuShao（秋梢期）**：秋季新梢生长期

##### 1.2.4 荔枝虫害分类数据集

**位置**：`Datasets/Classification/LycheeInsectPests/`

**用途**：训练荔枝虫害识别模型（7类虫害）

**数据量**：
- 训练集：517张图像
- 测试集：132张图像

**类别**：
1. **DaXiShuai（大蟋蟀）**
2. **FeiZhouLouGu（非洲蝼蛄）**
3. **LiZhiChun（荔枝蝽）**
4. **LongYanJi（龙眼鸡）**
5. **MaPiChun（麻皮蝽）**
6. **PoChun（珀蝽）**
7. **XingTianNiu（星天牛）**

##### 1.2.5 编码对照表

**位置**：`编码/`

**用途**：提供各类别的中英文对照和编码

**文件说明**：
- `total.csv` / `total.xlsx`：汇总所有类别的编码对照表
- `fruit.csv` / `水果对应的编码.xlsx`：19种水果的编码对照（包含荔枝）
- `病害对应编码.xlsx`：各类病害的编码对照
- `花卉对应编码.xlsx`：各类花卉的编码对照
- `虫害对应编码.xlsx`：各类虫害的编码对照（包含7种荔枝虫害）

**编码格式示例**：
```
code,category_zh,category,job
AP00000009,荔枝,Lychee,Fruit
IP00000150,大蟋蟀,DaXiShuai,LycheeInsectPests
```

#### 1.3 数据处理脚本

##### 1.3.1 数据集划分脚本

**分类数据集划分**：`Classification/split_plain.py`
- 功能：将Plain目录下的原始数据按80:20比例划分为训练集和测试集
- 输入：Plain目录下的分类图像文件夹
- 输出：对应的数据集train/test目录

**检测数据集划分**：`Detection/Plain/split.py`
- 功能：将LycheeRipe原始数据按80:20比例划分为训练集和验证集
- 输入：`LycheeRipe/images/`和`LycheeRipe/labels/`
- 输出：`LycheeRipeDetection/images/train/val/`和`LycheeRipeDetection/labels/train/val/`

##### 1.3.2 标签格式转换脚本

**分割转检测**：`Detection/CanopyDetection/seg_2_box.py`
- 功能：将分割标签（多边形坐标）转换为检测标签（边界框）
- 输入：分割格式标签文件（多边形顶点坐标）
- 输出：YOLO格式标签文件（边界框坐标）

##### 1.3.3 数据收集脚本

**相机数据收集**：`Detection/Plain/LycheeCamera/main.py`
- 功能：从相机采集的原始数据中收集特定成熟度阶段的图像
- 功能：根据日期映射表自动识别成熟度阶段并收集图像

#### 1.4 数据集统计信息

| 数据集类型 | 数据集名称 | 训练集 | 测试/验证集 | 类别数 | 用途 |
|----------|----------|--------|------------|--------|------|
| **检测** | LycheeRipeDetection | 295张 | 101张 | 4类 | **荔枝成熟度检测（核心）** |
| **检测** | CanopyDetection | 272张 | 116张 | 1类 | 树冠检测 |
| **检测** | Citrus-CidDet-YOLO | 391张 | 69张/119张 | 2类 | 柑橘检测 |
| **分类** | LycheePhenologicalStages | 783张 | 198张 | 5类 | 荔枝物候期分类 |
| **分类** | LycheeInsectPests | 517张 | 132张 | 7类 | 荔枝虫害分类 |
| **分类** | Fruit | 1429张 | 470张 | 19类 | 水果分类（含荔枝） |
| **分类** | CitrusDisease | 599张 | 154张 | 10类 | 柑橘病害分类 |
| **分类** | TeaDisease | 5127张 | 1288张 | - | 茶叶病害分类 |
| **分类** | Flower | 6497张 | 1676张 | - | 花卉分类 |
| **分割** | CanopySegmentation | 272张 | 116张 | 1类 | 树冠分割 |

#### 1.5 使用建议

1. **荔枝成熟度检测训练**：使用`LycheeRipeDetection/`数据集
   - 配置文件：`data.yaml`
   - 数据已划分为train/val，可直接用于训练

2. **数据预处理**：使用`Plain/LycheeCamera/main.py`收集特定阶段的数据

3. **数据集扩充**：可以参考其他数据集的结构和标注格式

4. **编码查询**：查看`编码/`目录下的CSV/Excel文件获取类别编码信息

### 2. yolov8 (`e:\基于无人机与AI的荔枝产业应用\yolov8`)

该文件夹包含了YOLOv8模型框架、荔枝相关数据集、训练脚本和推理脚本，是荔枝物候期识别和病虫害检测的核心工作目录。

#### 2.1 完整目录结构

```
yolov8/
├── datasets/                                    # 荔枝数据集目录
│   ├── lizhi_bugs/                             # 荔枝病虫害检测数据集（11类）
│   │   ├── chunx/                              # 荔枝椿象（蝽象）数据集
│   │   │   ├── images/
│   │   │   │   ├── train/                     # 训练集图像（73张）
│   │   │   │   └── val/                       # 验证集图像（32张）
│   │   │   ├── jsons/                         # JSON格式标注文件（105个）
│   │   │   └── labels/
│   │   │       ├── train/                     # 训练集标签（73个txt文件）
│   │   │       └── val/                       # 验证集标签（32个txt文件）
│   │   │
│   │   ├── dizhuc/                            # 蒂蛀虫数据集
│   │   │   ├── images/
│   │   │   │   ├── train/                     # 训练集图像（18张）
│   │   │   │   └── val/                       # 验证集图像（9张）
│   │   │   ├── jsons/                         # JSON格式标注文件（27个）
│   │   │   └── labels/
│   │   │       ├── train/                     # 训练集标签（18个txt文件）
│   │   │       └── val/                       # 验证集标签（9个txt文件）
│   │   │
│   │   ├── lieguo/                            # 裂果数据集
│   │   │   ├── images/
│   │   │   │   ├── train/                     # 训练集图像（28张）
│   │   │   │   └── val/                       # 验证集图像（12张）
│   │   │   ├── jsons/                         # JSON格式标注文件（40个）
│   │   │   └── labels/
│   │   │       ├── train/                     # 训练集标签（28个txt文件）
│   │   │       └── val/                       # 验证集标签（12个txt文件）
│   │   │
│   │   ├── longyji/                           # 龙眼鸡数据集
│   │   │   ├── images/
│   │   │   │   ├── train/                     # 训练集图像（49张）
│   │   │   │   └── val/                       # 验证集图像（22张）
│   │   │   ├── jsons/                         # JSON格式标注文件（71个）
│   │   │   └── labels/
│   │   │       ├── train/                     # 训练集标签（49个txt文件）
│   │   │       └── val/                       # 验证集标签（22个txt文件）
│   │   │
│   │   ├── shuangym/                          # 霜疫霉病数据集
│   │   │   ├── images/
│   │   │   │   ├── train/                     # 训练集图像（7张）
│   │   │   │   └── val/                       # 验证集图像（4张）
│   │   │   ├── jsons/                         # JSON格式标注文件（11个）
│   │   │   └── labels/                        # YOLO格式标签文件（11个txt文件）
│   │   │
│   │   ├── tanju/                             # 炭疽病数据集
│   │   │   ├── images/
│   │   │   │   ├── train/                     # 训练集图像（18张）
│   │   │   │   └── val/                       # 验证集图像（9张）
│   │   │   ├── jsons/                         # JSON格式标注文件（26个）
│   │   │   └── labels/                        # YOLO格式标签文件（27个txt文件）
│   │   │
│   │   ├── yanmei/                            # 煤烟病数据集
│   │   │   ├── images/
│   │   │   │   ├── train/                     # 训练集图像（8张）
│   │   │   │   └── val/                       # 验证集图像（5张）
│   │   │   ├── jsons/                         # JSON格式标注文件（14个）
│   │   │   └── labels/                        # YOLO格式标签文件（13个txt文件）
│   │   │
│   │   ├── images/                            # 合并后的病虫害图像
│   │   │   ├── train/                         # 训练集图像（206张）
│   │   │   └── val/                           # 验证集图像（94张）
│   │   │
│   │   ├── labels/                            # 合并后的病虫害标签
│   │   │   ├── train/                         # 训练集标签（205个txt文件）
│   │   │   └── val/                           # 验证集标签（94个txt文件）
│   │   │
│   │   └── 使用说明.txt                        # 病虫害类别编码说明
│   │
│   └── lizhi_period/                          # 荔枝物候期检测数据集（8类）
│       ├── images/                            # 合并后的物候期图像
│       │   ├── train/                         # 训练集图像（73张）
│       │   └── val/                           # 验证集图像（35张）
│       │
│       ├── images-1 到 images-8/              # 不同批次/阶段的物候期图像
│       │   ├── train/                         # 各批次的训练集图像
│       │   └── val/                           # 各批次的验证集图像
│       │
│       ├── images-old/                        # 旧版本物候期图像
│       │   ├── train/                         # 旧版本训练集图像（88张）
│       │   └── val/                           # 旧版本验证集图像（39张）
│       │
│       ├── labels/                            # 合并后的物候期标签
│       │   ├── train/                         # 训练集标签（73个txt文件）
│       │   └── val/                           # 验证集标签（35个txt文件）
│       │
│       ├── labels-1 到 labels-8/              # 不同批次/阶段的物候期标签
│       │   ├── train/                         # 各批次的训练集标签
│       │   └── val/                           # 各批次的验证集标签
│       │
│       ├── labels-old/                        # 旧版本物候期标签
│       │   ├── train/                         # 旧版本训练集标签（88个txt文件）
│       │   └── val/                           # 旧版本验证集标签（39个txt文件）
│       │
│       ├── svn_images/                        # SVN版本控制的图像
│       │   ├── train/                         # 训练集图像（47张）
│       │   └── val/                           # 验证集图像（22张）
│       │
│       ├── svn_labels/                        # SVN版本控制的标签
│       │   ├── train/                         # 训练集标签（47个txt文件）
│       │   └── val/                           # 验证集标签（22个txt文件）
│       │
│       ├── train/                             # 物候期训练集（按类别分类）
│       │   ├── 1/                             # 类别1：末次秋梢抽发期
│       │   ├── 2/                             # 类别2：末次秋梢老熟期
│       │   ├── 3/                             # 类别3：白点期
│       │   ├── 4/                             # 类别4：花芽分化期
│       │   ├── 5/                             # 类别5：雌花和雄花开放期
│       │   ├── 6/                             # 类别6：生理落果高峰期
│       │   ├── 7/                             # 类别7：果实快速膨大期
│       │   └── 8/                             # 类别8：成熟期
│       │
│       ├── val/                               # 物候期验证集（按类别分类）
│       │   ├── 1/                             # 类别1
│       │   ├── 2/                             # 类别2
│       │   └── ...                            # 类别3-8
│       │
│       └── README.txt                         # 物候期数据集说明
│
├── ultralytics-main/                          # YOLOv8核心代码库（Ultralytics官方库）
│   ├── ultralytics/                           # YOLOv8核心源代码
│   │   ├── cfg/
│   │   │   └── datasets/
│   │   │       ├── lizhi-dt_period.yaml       # 物候期检测数据集配置文件
│   │   │       └── lizhi-dt_bugs.yaml         # 病虫害检测数据集配置文件
│   │   ├── models/                            # 模型定义
│   │   ├── engine/                            # 训练、验证、推理引擎
│   │   ├── data/                              # 数据处理模块
│   │   ├── utils/                             # 工具函数
│   │   └── ...
│   │
│   ├── runs/                                  # 训练结果保存目录
│   │   └── detect/
│   │       ├── train6/                        # 物候期检测训练结果（第6次训练）
│   │       │   ├── weights/
│   │       │   │   ├── best.pt                # 最佳模型（用于推理）
│   │       │   │   └── last.pt                # 最后一轮模型
│   │       │   ├── results.png                # 训练过程可视化
│   │       │   ├── confusion_matrix.png       # 混淆矩阵
│   │       │   └── ...
│   │       │
│   │       ├── train30/                       # 病虫害检测训练结果（第30次训练）
│   │       │   ├── weights/
│   │       │   │   ├── best.pt                # 最佳模型（用于推理）
│   │       │   │   └── last.pt                # 最后一轮模型
│   │       │   ├── results.png                # 训练过程可视化
│   │       │   ├── confusion_matrix.png       # 混淆矩阵
│   │       │   └── ...
│   │       │
│   │       └── predict/                       # 推理结果保存目录
│   │           └── *.jpg                      # 标注后的推理结果图像
│   │
│   ├── runPeriod_detection.py                 # 物候期检测模型训练脚本
│   ├── runBugs_detection.py                   # 病虫害检测模型训练脚本
│   ├── Predict_Period.py                      # 物候期识别推理脚本（标准方法）
│   ├── Predict_Period-sahi.py                 # 物候期识别推理脚本（SAHI方法）
│   ├── Predict_Bugs.py                        # 病虫害识别推理脚本（标准方法）
│   ├── Predict_Bugs-sahi.py                   # 病虫害识别推理脚本（SAHI方法）
│   ├── runTransformOnnx.py                    # 模型格式转换脚本（.pt → .onnx）
│   ├── yolov8n.pt                             # YOLOv8n预训练模型（检测任务）
│   ├── yolov8n-cls.pt                         # YOLOv8n预训练模型（分类任务）
│   ├── README.md                               # 项目说明文档
│   ├── README.zh-CN.md                        # 项目说明文档（中文）
│   └── ...
│
├── svn_dataprocess/                           # 数据处理脚本目录（版本控制）
│   ├── 1-data_crawler.py                      # 图像数据爬取脚本
│   ├── 1-bugdata_clawer.py                    # 病虫害数据爬取脚本
│   ├── 2-json_txt.py                          # JSON格式转YOLO格式标签脚本
│   ├── 3-data_split.py                        # 数据集划分脚本（train/val）
│   ├── data_process.py                        # 数据预处理脚本（文件重命名等）
│   ├── data_transform.py                      # 数据格式转换脚本（分类任务）
│   ├── jpeg_jpg.py                            # 图像格式统一脚本（JPEG→JPG）
│   │
│   ├── bug-datasets/                          # 爬取的病虫害原始数据
│   │   ├── chunx/                             # 椿象原始数据（105张图像+标注）
│   │   ├── dizhuc/                            # 蒂蛀虫原始数据（27张图像+标注）
│   │   ├── lieguo/                            # 裂果原始数据（40张图像+标注）
│   │   ├── longyji/                           # 龙眼鸡原始数据（71张图像+标注）
│   │   ├── shuangym/                          # 霜疫霉病原始数据（11张图像+标注）
│   │   ├── tanju/                             # 炭疽病原始数据（26张图像+标注）
│   │   ├── yanmei/                            # 煤烟病原始数据（14张图像+标注）
│   │   └── 使用说明.txt                        # 病虫害类别编码说明
│   │
│   ├── Period-sets/                           # 物候期原始数据集（按阶段分类）
│   │   ├── Period-1/                          # 末次秋梢抽发期（11张图像+标注）
│   │   ├── Period-2/                          # 末次秋梢老熟期（8张图像+标注）
│   │   ├── Period-3/                          # 白点期（14张图像+标注）
│   │   ├── Period-4/                          # 花芽分化期（8张图像+标注）
│   │   ├── Period-5/                          # 雌花和雄花开放期（14张图像+标注）
│   │   ├── Period-6/                          # 生理落果高峰期（15张图像+标注）
│   │   ├── Period-7/                          # 果实快速膨大期（19张图像+标注）
│   │   └── Period-8/                          # 成熟期（11张图像+标注）
│   │
│   ├── lizhi_period/                          # 物候期原始数据
│   │   ├── images/                            # 原始图像（50张）
│   │   ├── jsons/                             # JSON格式标注文件（50个）
│   │   └── labels/                            # YOLO格式标签文件（50个txt文件）
│   │
│   ├── clawer/                                # 爬取的图像（按来源分类）
│   │   ├── pic01/                             # 来源1（51张图像）
│   │   ├── pic02/                             # 来源2（56张图像）
│   │   ├── pic03/                             # 来源3（7张图像）
│   │   ├── pic04/                             # 来源4（22张图像）
│   │   ├── pic05/                             # 来源5（25张图像）
│   │   ├── pic06/                             # 来源6（27张图像）
│   │   ├── pic07/                             # 来源7（34张图像）
│   │   └── pic08/                             # 来源8（131张图像）
│   │
│   ├── pic_labeling/                          # 标注后的图像和标签
│   │   ├── images/                            # 标注用的图像（127张）
│   │   ├── jsons/                             # LabelMe格式标注文件（127个）
│   │   └── labels/                            # YOLO格式标签文件（127个txt文件）
│   │
│   └── svn_pic/                               # SVN版本控制的原始图像（50张）
│
├── resemble_period.py                         # 物候期数据集合并脚本
├── resemble_bugs.py                           # 病虫害数据集合并脚本
├── 使用说明.ipynb                              # Jupyter Notebook完整使用说明
├── 历史记录01使用说明                          # 历史使用说明文档
├── 历史记录02周期识别                          # 周期识别历史记录
└── 历史记录03病虫害识别                        # 病虫害识别历史记录
```

#### 2.2 关键文件说明

##### 2.2.1 训练脚本

**物候期检测训练**：`ultralytics-main/runPeriod_detection.py`
- **功能**：训练荔枝物候期识别模型（8个阶段）
- **数据集配置**：`ultralytics/cfg/datasets/lizhi-dt_period.yaml`
- **数据集路径**：`datasets/lizhi_period/`
- **训练输出**：`runs/detect/train6/weights/best.pt`

**病虫害检测训练**：`ultralytics-main/runBugs_detection.py`
- **功能**：训练荔枝病虫害识别模型（11类）
- **数据集配置**：`ultralytics/cfg/datasets/lizhi-dt_bugs.yaml`
- **数据集路径**：`datasets/lizhi_bugs/`
- **训练输出**：`runs/detect/train30/weights/best.pt`

##### 2.2.2 推理脚本

**物候期识别推理**：
- `ultralytics-main/Predict_Period.py`：标准推理方法（推荐）
- `ultralytics-main/Predict_Period-sahi.py`：SAHI推理方法（处理大图像，不常用）

**病虫害识别推理**：
- `ultralytics-main/Predict_Bugs.py`：标准推理方法（推荐）
- `ultralytics-main/Predict_Bugs-sahi.py`：SAHI推理方法（处理大图像，不常用）

##### 2.2.3 数据处理脚本

**数据集合并脚本**：
- `resemble_period.py`：合并物候期数据集的多个批次（images-1到images-8）到统一的train/val目录
- `resemble_bugs.py`：合并病虫害数据集的多个类别（chunx, dizhuc等）到统一的train/val目录

**数据预处理脚本**（`svn_dataprocess/`目录）：
- `1-data_crawler.py`：从网络爬取图像数据
- `1-bugdata_clawer.py`：爬取病虫害图像数据
- `2-json_txt.py`：将LabelMe格式（JSON）转换为YOLO格式（TXT）
- `3-data_split.py`：将数据集按80:20划分为训练集和验证集
- `data_process.py`：数据预处理（文件重命名等）
- `data_transform.py`：数据格式转换（分类任务）
- `jpeg_jpg.py`：统一图像格式（JPEG→JPG）

##### 2.2.4 模型转换脚本

**模型导出**：`ultralytics-main/runTransformOnnx.py`
- **功能**：将PyTorch模型（.pt）转换为ONNX格式（.onnx）
- **优点**：推理速度更快，更容易部署

#### 2.3 数据集配置文件

##### 2.3.1 物候期检测数据集配置

**文件位置**：`ultralytics-main/ultralytics/cfg/datasets/lizhi-dt_period.yaml`

**内容**：
```yaml
path: D:/yolov8/datasets/lizhi_period
train: D:/yolov8/datasets/lizhi_period/images/train/
val: D:/yolov8/datasets/lizhi_period/images/val/
nc: 8
names:
  0 : 1  # 末次秋梢抽发期
  1 : 2  # 末次秋梢老熟期
  2 : 3  # 白点期
  3 : 4  # 花芽分化期
  4 : 5  # 雌花和雄花开放期
  5 : 6  # 生理落果高峰期
  6 : 7  # 果实快速膨大期
  7 : 8  # 成熟期
```

**8个物候期阶段**（华农标准）：
1. **末次秋梢抽发期**：发芽的树尖儿
2. **末次秋梢老熟期**：新长的并且变绿色的树叶
3. **白点期**：花芽含苞待放的白点
4. **花芽分化期**：花芽打开了，但还不算夸张
5. **雌花和雄花开放期**：花芽完全打开，并且颜色发黄
6. **生理落果高峰期**：结出小型的果实
7. **果实快速膨大期**：长方形、偏圆形的果实，青色
8. **成熟期**：圆形果实，红色

##### 2.3.2 病虫害检测数据集配置

**文件位置**：`ultralytics-main/ultralytics/cfg/datasets/lizhi-dt_bugs.yaml`

**内容**：
```yaml
path: D:/yolov8/datasets/lizhi_bugs
train: D:/yolov8/datasets/lizhi_bugs/images/train/
val: D:/yolov8/datasets/lizhi_bugs/images/val/
nc: 11
names:
  0 : 1   # 霜疫霉病
  1 : 2   # 炭疽病（叶子）
  2 : 3   # 煤烟病（叶子）
  3 : 4   # 裂果
  4 : 5   # 荔枝椿象（臭屁虫，除卵外）
  5 : 6   # 蒂蛀虫
  6 : 7   # 龙眼鸡
  7 : 8   # 椿象卵
  8 : 9   # 煤烟病（果子）
  9 : 10  # 炭疽病（果子）
  10 : 11 # 蒂蛀虫（虫蛹）
```

**11类病虫害编码**：
- **霜疫霉病**：类别ID 1
- **炭疽病**：叶子为类别ID 2，果子为类别ID 10
- **煤烟病**：叶子为类别ID 3，果子为类别ID 9
- **裂果**：类别ID 4
- **荔枝椿象（臭屁虫）**：除卵外为类别ID 5，卵为类别ID 8
- **蒂蛀虫**：标记为类别ID 6，结成蛹为类别ID 11
- **龙眼鸡**：类别ID 7

#### 2.4 数据集统计信息

##### 2.4.1 物候期检测数据集（lizhi_period）

| 数据来源 | 训练集 | 验证集 | 总计 |
|---------|--------|--------|------|
| **合并后的数据集** | 73张 | 35张 | 108张 |
| **images-old** | 88张 | 39张 | 127张 |
| **images-1到images-8** | 73张 | 35张 | 108张 |
| **svn_images** | 47张 | 22张 | 69张 |

**类别数**：8类（8个物候期阶段）

##### 2.4.2 病虫害检测数据集（lizhi_bugs）

| 病虫害类别 | 训练集 | 验证集 | 总计 |
|----------|--------|--------|------|
| **chunx（荔枝椿象）** | 73张 | 32张 | 105张 |
| **dizhuc（蒂蛀虫）** | 18张 | 9张 | 27张 |
| **lieguo（裂果）** | 28张 | 12张 | 40张 |
| **longyji（龙眼鸡）** | 49张 | 22张 | 71张 |
| **shuangym（霜疫霉病）** | 7张 | 4张 | 11张 |
| **tanju（炭疽病）** | 18张 | 9张 | 27张 |
| **yanmei（煤烟病）** | 8张 | 5张 | 13张 |
| **合并后的数据集** | 206张 | 94张 | 300张 |

**类别数**：11类（11种病虫害类型）

#### 2.5 训练模型位置

**物候期识别最佳模型**：
- 路径：`ultralytics-main/runs/detect/train6/weights/best.pt`
- 用途：荔枝物候期识别（8个阶段）
- 使用：在`Predict_Period.py`中加载此模型进行推理

**病虫害识别最佳模型**：
- 路径：`ultralytics-main/runs/detect/train30/weights/best.pt`
- 用途：荔枝病虫害识别（11类）
- 使用：在`Predict_Bugs.py`中加载此模型进行推理

#### 2.6 数据处理流程

##### 2.6.1 物候期数据处理流程

```
1. 数据爬取
   └── svn_dataprocess/1-data_crawler.py
       ↓
2. 数据预处理
   └── svn_dataprocess/data_process.py（文件重命名）
       └── svn_dataprocess/jpeg_jpg.py（格式统一）
       ↓
3. 数据标注（LabelMe）
   └── 生成JSON格式标注文件
       ↓
4. 格式转换
   └── svn_dataprocess/2-json_txt.py（JSON → YOLO TXT）
       ↓
5. 数据集划分
   └── svn_dataprocess/3-data_split.py（80:20划分）
       ↓
6. 数据集合并（如有多个批次）
   └── resemble_period.py（合并images-1到images-8）
       ↓
7. 训练准备
   └── 配置lizhi-dt_period.yaml
       ↓
8. 模型训练
   └── runPeriod_detection.py
```

##### 2.6.2 病虫害数据处理流程

```
1. 数据爬取
   └── svn_dataprocess/1-bugdata_clawer.py
       ↓
2. 数据标注（LabelMe）
   └── 按类别组织数据（chunx, dizhuc等）
       ↓
3. 格式转换
   └── svn_dataprocess/2-json_txt.py（JSON → YOLO TXT）
       ↓
4. 数据集划分
   └── svn_dataprocess/3-data_split.py（每个类别80:20划分）
       ↓
5. 数据集合并
   └── resemble_bugs.py（合并所有类别到统一目录）
       ↓
6. 训练准备
   └── 配置lizhi-dt_bugs.yaml
       ↓
7. 模型训练
   └── runBugs_detection.py
```

#### 2.7 使用说明

##### 2.7.1 环境配置

根据`使用说明.ipynb`和`历史记录01使用说明`，需要配置：

1. **CUDA + CuDNN + PyTorch**：
   - 安装顺序：CUDA → CuDNN → PyTorch
   - 注意版本兼容性

2. **安装ultralytics**：
   ```bash
   pip install ultralytics
   ```

3. **克隆项目**：
   ```bash
   git clone [项目路径]
   ```

4. **测试**：确保GPU可用

##### 2.7.2 训练流程

1. **准备数据集**：
   - 将数据集放在`datasets/`目录下
   - 确保图像和标签文件对应

2. **配置数据集**：
   - 修改`ultralytics/cfg/datasets/lizhi-dt_period.yaml`（物候期）
   - 或修改`ultralytics/cfg/datasets/lizhi-dt_bugs.yaml`（病虫害）

3. **运行训练**：
   ```bash
   # 物候期识别训练
   cd ultralytics-main
   python runPeriod_detection.py
   
   # 病虫害识别训练
   python runBugs_detection.py
   ```

4. **查看结果**：
   - 训练结果保存在`runs/detect/trainX/`目录
   - 最佳模型：`runs/detect/trainX/weights/best.pt`

##### 2.7.3 推理流程

1. **加载模型**：
   ```python
   from ultralytics import YOLO
   model = YOLO("runs/detect/train6/weights/best.pt")  # 物候期模型
   # 或
   model = YOLO("runs/detect/train30/weights/best.pt")  # 病虫害模型
   ```

2. **运行推理**：
   ```bash
   # 物候期识别推理
   python Predict_Period.py
   
   # 病虫害识别推理
   python Predict_Bugs.py
   ```

3. **查看结果**：
   - 推理结果保存在`runs/detect/predict/`目录
   - 结果图像会自动标注检测框和类别

##### 2.7.4 模型导出

如果需要将模型导出为ONNX格式（用于部署）：

```bash
python runTransformOnnx.py
```

**注意**：需要先修改脚本中的模型路径。

#### 2.8 关键脚本详解

##### 2.8.1 物候期数据集合并脚本（resemble_period.py）

**功能**：将多个批次的物候期数据集（images-1到images-8）合并到统一的train/val目录

**工作原理**：
1. 遍历`images-1`到`images-8`文件夹
2. 将每个文件夹中的train和val图像复制到统一的`images/train/`和`images/val/`目录
3. 同时复制对应的标签文件到`labels/train/`和`labels/val/`目录

**使用场景**：当数据集分多批次收集时，需要合并后才能训练

##### 2.8.2 病虫害数据集合并脚本（resemble_bugs.py）

**功能**：将多个类别的病虫害数据集（chunx, dizhuc等）合并到统一的train/val目录

**工作原理**：
1. 遍历各个病虫害类别文件夹（chunx, dizhuc, lieguo等）
2. 将每个类别的train和val图像复制到统一的`images/train/`和`images/val/`目录
3. 同时复制对应的标签文件到`labels/train/`和`labels/val/`目录

**使用场景**：当每个病虫害类别单独组织时，需要合并后才能进行多类别训练

#### 2.9 注意事项

1. **数据集路径**：
   - 配置文件中的路径是绝对路径（如`D:/yolov8/datasets/lizhi_period`）
   - 如果项目路径不同，需要修改配置文件

2. **类别名称**：
   - YOLO训练时主要使用类别ID（0, 1, 2...）
   - 类别名称在配置文件中设置，主要用于可视化显示

3. **模型文件**：
   - `best.pt`是验证集上表现最好的模型（推荐用于推理）
   - `last.pt`是最后一轮训练的模型（可能不如best.pt）

4. **数据标注**：
   - 使用LabelMe进行标注，生成JSON格式
   - 然后使用`2-json_txt.py`转换为YOLO格式
   - YOLO格式标签文件名必须与图像文件名对应

5. **推理方法**：
   - 标准方法（Predict_Period.py）：适合大多数场景
   - SAHI方法（Predict_Period-sahi.py）：适合处理大图像，速度较慢，不常用

---

## 附录：快速参考（完整版）

### A. 训练命令

#### A1. 成熟度检测模型训练
```bash
cd yolov8/ultralytics-main
python runPeriod_detection.py
```

#### A2. 物候期检测模型训练（小目标优化）
```python
from ultralytics import YOLO

model = YOLO("yolov8n.pt")
model.train(
    data="new_dataset/data.yaml",
    epochs=300,
    imgsz=1280,  # 小目标优化：使用1280×1280
    batch=8,
    conf=0.3,
    copy_paste=0.3,
    name="lychee_phenological_1280"
)
```

### B. 推理命令

#### B1. PyTorch模型推理（标准）
```python
from ultralytics import YOLO

# 加载模型
model = YOLO("runs/detect/train6/weights/best.pt")  # 物候期模型
# 或
model = YOLO("runs/detect/lychee_ripe_detection/weights/best.pt")  # 成熟度模型

# 推理
results = model.predict(
    source="image.jpg",
    imgsz=1280,  # 小目标：使用1280×1280
    conf=0.3,    # 小目标：降低置信度
    save=True
)
```

#### B2. ONNX模型推理
```python
from detect import Detector

detector = Detector(
    model_path="models/v5.onnx",
    conf_threshold=0.5,
    iou_threshold=0.5
)

results = detector.detect("image.jpg")
```

#### B3. 图像切片推理（大图像小目标）
```python
# 参考"十一、小目标检测优化方案"章节中的完整代码
# 适合1920×1080图像中的小目标检测
```

### C. API服务

```bash
cd LycheeDetection-master/LycheeDetection-master
fastapi run --workers 4 main.py
```

### D. 模型导出

```python
from ultralytics import YOLO

model = YOLO("runs/detect/train6/weights/best.pt")
success = model.export(format="onnx", simplify=True)
print("转换成功" if success else "转换失败")
```

### E. 数据质量检查

```python
# 检查标注的目标大小分布
python check_annotation_sizes.py
```

---

**文档版本**：v2.0  
**最后更新**：2026-01-20  
**适用项目**：
- 荔枝成熟度检测（Immature、Turing、Coloring、Ripe）
- 荔枝物候期检测（秋梢期、白点期等）
- 小目标检测优化方案

**主要更新**：
- v2.0（2026-01-20）：新增小目标检测优化方案章节，包含1920×1080图像中小目标的训练优化方案
- v1.0（2026-01-19）：初始版本，包含基础训练和推理指南

**文档结构**：
1. 基础概念（训练、推理、类别说明）
2. 整体工作流程
3. 训练流程详解
4. 推理流程详解
5. 完整使用示例
6. 常见问题解答
7. 术语表
8. 扩展阅读
9. 总结
10. 实际应用场景与扩展
11. 小目标检测优化方案（秋梢期与白点期）
12. 附录：快速参考
13. 附录：文件夹结构说明

如有问题，请参考代码注释或联系项目维护者。
